# Unpatched CVEs against Ubuntu 24.04 noble

This file tracks CVEs surfaced by safelibs port `relevant_cves.json`
files that were excluded from the regression testcase set because the
Ubuntu 24.04 noble baseline still exhibits the vulnerable behavior.
Each row records the library, CVE id, upstream evidence, and the
observation that prevented the testcase from passing on `original`.

| Library | CVE | Reason | Upstream evidence | Observation |
| --- | --- | --- | --- | --- |
| libjson | CVE-2013-6371 | not_reproducible_offline | json-c hash collision DoS fixed in 0.12 by salting string hashing with a random seed | No deterministic offline reproducer available — exercising the worst-case bucket distribution requires knowing the upstream hash function and crafting collisions that generalize across noble's libjson-c5 package; treated as covered structurally rather than as an executable regression. |
| libjansson | CVE-2013-6401 | not_reproducible_offline | Jansson 2.6 changed hash function and seeded hashes with random data | Hash-flooding regression cannot be encoded as a deterministic offline test against a randomized hasher; observable behavior on a single input is identical between vulnerable and patched builds. |
| libuv | CVE-2014-9748 | not_reproducible_offline | uv_rwlock_t fallback affected only Windows XP / Server 2003 | Linux harness does not exercise the Windows fallback rwlock path; the noble libuv1t64 build does not ship the affected code. |
| libuv | CVE-2015-0278 | not_reproducible_offline | Privilege drop in uv_spawn did not clear supplementary groups | A meaningful reproducer requires running as root, calling setgroups, and observing supplementary-group leakage in the spawned child; the validator harness does not have privileged container capabilities. |
| libuv | CVE-2024-22017 | not_reproducible_offline | setuid did not affect libuv's pre-initialized io_uring submissions | Demonstrating retained privileges across setuid requires an io_uring-capable kernel and a privileged-to-unprivileged transition that the noble container harness does not provide. |
| libxml | CVE-2008-4409 | not_reproducible_offline | Crafted predefined-entity definitions could exhaust memory | Public reproducers rely on internals of older libxml2 entity-table allocation; modern noble libxml2 has rewritten this path and there is no published canonical input that asserts patched behavior offline. |
| libxml | CVE-2012-0841 | not_reproducible_offline | Dictionary hash randomization mitigates collision DoS | Cannot construct deterministic adversarial keys against a randomized hash without knowledge of the per-process seed. |
| libxml | CVE-2013-0338 | not_reproducible_offline | Internal entity expansion linear-complexity DoS | The bug surfaces as wall-clock degradation on a vulnerable build; on a patched build the input parses identically to other billion-laughs variants, so a discriminating offline reproducer collapses into CVE-2003-1564 / CVE-2014-3660 rather than this CVE specifically. |
| libxml | CVE-2015-1819 | not_reproducible_offline | xmlReader streaming API failed constant-memory guarantee under entity expansion | Demonstrating the constant-memory regression requires measuring RSS over a long-running reader stream, which the matrix runner does not capture; the input itself is covered by the entity-expansion regressions. |
| libxml | CVE-2015-5312 | not_reproducible_offline | xmlStringLenDecodeEntities expansion-limit gap | Bypass converged with the general billion-laughs limits in upstream — same payloads now uniformly trigger the patched limit, so this CVE is not separable from CVE-2014-3660 in an offline test. |
| libxml | CVE-2015-8035 | not_reproducible_offline | XZ decompression error-handling loop did not terminate | Reproducer requires a crafted XZ-compressed XML whose error path drove the loop; the validator does not ship the malformed-xz fixture and constructing one offline that distinguishes vulnerable from patched behavior is not deterministic. |
| libxml | CVE-2016-4449 | not_reproducible_offline | Entity decoding outside validating mode fetched external content | Public reproducers depend on a network endpoint or a non-default loader callback; default noble configuration is covered by the XXE regressions for CVE-2013-0339 and CVE-2017-7375 and adds no separable signal for this CVE specifically. |
| libxml | CVE-2016-9318 | not_reproducible_offline | API design: lacked a same-document-only resolution policy | This is an API-surface concern: there is no input that distinguishes vulnerable from patched behavior at the xmllint command level; the patched API options exist but exercising them does not assert past-CVE state. |
| libxml | CVE-2017-18258 | not_reproducible_offline | LZMA decompression had no memory limit | Reproducer requires a crafted XZ-wrapped XML hitting xmlIO LZMA paths with attacker-driven memory allocation; no canonical fixture is shipped with the validator and constructing a discriminating one offline is not deterministic. |
| libxml | CVE-2018-9251 | not_reproducible_offline | LZMA memlimit-error infinite loop in libxml2 IO | Same shape as CVE-2017-18258 — distinguishing patched from vulnerable behavior requires an XZ fixture crafted to trigger the memlimit error path, which is not part of the validator fixture set. |
| libxml | CVE-2018-14567 | not_reproducible_offline | Second LZMA infinite-loop variant in the same code path | Variant of CVE-2018-9251 against the same crafted-XZ fixture; sharing the same offline reproducer constraint. |
| libxml | CVE-2021-3537 | not_reproducible_offline | Mixed-content parser-error propagation | Bug fired only when a specific schema-validation pipeline observed an error after a non-fatal parse; the public reproducer relies on internal reset paths that the patched libxml2 has refactored and no canonical input asserts the patched control flow offline. |
| libxml | CVE-2023-28484 | not_reproducible_offline | xmlSchemaFixupComplexType nullability bug | Public reproducer depends on a specific malformed XSD whose canonical form is not part of the validator fixture set; a discriminating offline input could not be constructed without risking false negatives on noble. |
| libxml | CVE-2025-27113 | not_reproducible_offline | xmlPatMatch nullability crash | The CVE record gives no concrete reproducer input; the patch added defensive null checks but no canonical malformed input is published, so an offline regression cannot distinguish vulnerable from patched builds. |
| libxml | CVE-2025-49795 | not_reproducible_offline | XPath null dereference on crafted expressions | Public reports describe the bug class but do not publish a deterministic input; XPath fuzz reproducers vary across libxml2 minor versions. |
| libxml | CVE-2026-0992 | not_reproducible_offline | Repeated nextCatalog traversal CPU exhaustion | The CPU-exhaustion regression requires measuring wall-clock degradation over many catalog hops; on a patched build the same input parses quickly, so the offline test collapses into CVE-2026-0990 rather than asserting this CVE separately. |
| liblzma | CVE-2024-3094 | not_reproducible_offline | Embedded malicious code in 5.6.0/5.6.1 release tarballs altered liblzma during the build | Bug class is supply-chain compromise of the release pipeline rather than a runtime parser path. Noble shipped 5.6.1+really5.4.5 (the revert), so the affected object code is absent and no input distinguishes vulnerable from patched at the library API level. |
| libbz2 | CVE-2005-1260 | not_reproducible_offline | Decompression-bomb / infinite-loop handling in BZ2_decompress before 1.0.3 | The historical PoC payload that drove the infinite loop is not part of the validator fixture set, and a hand-rolled corrupted bz2 stream is rejected identically by both the vulnerable and the patched library at the user-visible level — only wall-clock divergence distinguishes them, which the matrix runner does not measure. |
| libbz2 | CVE-2010-0405 | not_reproducible_offline | Integer overflow in BZ2_decompress selector / Huffman accounting before 1.0.6 | The exact bit-level fixture that exercises the unchecked nSelectors arithmetic is not published, and a generic corrupted bz2 stream is rejected by both vulnerable and patched libraries with identical user-visible diagnostics; constructing a discriminating offline reproducer would require bit-perfect reconstruction of the Huffman / selector layout. |
| libarchive | CVE-2026-5121 | not_reproducible_offline | 32-bit zisofs block-pointer allocation overflow in the ISO9660 reader | The allocation-overflow path is gated on a 32-bit target; the noble validator runs amd64 containers where the multiplication does not overflow, so a discriminating offline reproducer would require a 32-bit harness which the validator does not provide. |
| libarchive | CVE-2026-4426 | not_reproducible_offline | zisofs `pz_log2_bs` validation in the ISO9660 Rock Ridge extension reader | Reproducer requires a crafted ISO9660 image with a specific Rock Ridge zisofs extension setting an out-of-range `pz_log2_bs`; binary ISO fixture is not part of the validator inputs and cannot be reconstructed reliably from the CVE record alone. |
| libarchive | CVE-2026-4424 | not_reproducible_offline | RAR LZSS sliding-window resize across compression-method transitions | The bug only surfaces inside the proprietary RAR decompressor on a multi-method archive; constructing a minimal RAR with the exact compression-method transition that re-resizes the window would require bit-level RAR encoder knowledge that the validator does not have. |
| libarchive | CVE-2026-4111 | not_reproducible_offline | RAR5 `archive_read_data()` infinite loop on no-progress decompression state | Reproducer needs a structurally valid (checksum-passing) RAR5 archive that drives the decompressor into a no-progress state; the validator has no canonical RAR5 fixture and bsdtar would short-circuit any hand-built malformed RAR5 before reaching the no-progress code path. |
| libarchive | CVE-2025-5915 | not_reproducible_offline | RAR filter-block size versus LZSS-window validation | Same constraint as the other RAR-decoder CVEs — a discriminating fixture must be a valid RAR with a precisely-sized filter block exceeding the active LZSS window, and that input cannot be hand-rolled offline. |
| libarchive | CVE-2025-5914 | not_reproducible_offline | RAR `archive_read_format_rar_seek_data()` integer overflow | Crafted RAR seek-position fixture is not in the validator inputs; without an authentic RAR encoder the seek arithmetic cannot be exercised. |
| libarchive | CVE-2024-48958 | not_reproducible_offline | RAR delta-filter `src` cursor advancing beyond `dst` | Requires a crafted RAR with the filter metadata configured to drive cursor movement past the destination window; binary RAR construction is not feasible from the CVE record. |
| libarchive | CVE-2024-48957 | not_reproducible_offline | RAR audio-filter `src` cursor advancing beyond `dst` | Same RAR-encoder constraint as the delta-filter case. |
| libarchive | CVE-2023-30571 | not_reproducible_offline | `umask()` race in `archive_write_disk_posix.c` flipping process-wide umask to 0 | Bug class is a thread-race that requires a multithreaded extraction harness with one thread reading the global umask while another mutates it; the validator does not run libarchive in a multithreaded context. |
| libarchive | CVE-2021-31566 | not_reproducible_offline | Improper symlink-target resolution while applying metadata during extraction | Reproducer relies on a symlink that points outside the extraction root and on metadata operations that follow that link; constructing a deterministic offline test requires either privileged container capabilities or a filesystem mount that escapes the harness, neither of which the validator provides. |
| libarchive | CVE-2021-23177 | not_reproducible_offline | Improper link resolution during ACL application | Same containment-test setup as CVE-2021-31566, plus the additional dependency on POSIX ACL support; the validator does not exercise the ACL extraction path under controlled symlink conditions. |
| libarchive | CVE-2020-9308 | not_reproducible_offline | RAR5 zero-size header dispatch in `archive_read_support_format_rar5.c` | Hand-rolled RAR5 magic + zero-length header is accepted as an empty archive by the patched bsdtar (rc=0, no entries) and therefore does not exercise the formerly-vulnerable header-dispatch path; a real PoC needs a structurally valid RAR5 wrapper around the malformed header. |
| libarchive | CVE-2019-1000020 | not_reproducible_offline | ISO9660 Rock Ridge continuation infinite loop in `read_CE()` / `parse_rockridge()` | Reproducer is a binary ISO9660 image with a self-referential Rock Ridge continuation chain; the validator does not ship the fixture and constructing one bit-perfectly is not feasible. |
| libarchive | CVE-2016-8689 | not_reproducible_offline | 7zip `read_Header` mishandling of repeated EmptyStream attributes | 7zip header-dispatch fixture cannot be hand-rolled; the validator has no canonical malformed 7z input. |
| libarchive | CVE-2016-6250 | not_reproducible_offline | ISO9660 writer integer overflow in filename-length verification | The malformed-input branch is reachable only when an extraordinarily long filename is fed through `bsdtar --format iso9660`; on noble libarchive 3.7.2 the writer succeeds for hand-rolled long names without surfacing the bounds-validation diagnostic, so an offline test cannot distinguish the patched arithmetic from the vulnerable path. |
| libarchive | CVE-2016-5844 | not_reproducible_offline | ISO parser integer overflow in offset / length math | Crafted ISO9660 fixture with overflowing offset fields is not part of the validator inputs and cannot be hand-rolled with reasonable confidence. |
| libarchive | CVE-2016-4300 | not_reproducible_offline | 7zip `read_SubStreamsInfo` integer overflow on large substream count | 7zip header construction beyond the validator's tooling; no canonical fixture available. |
| libarchive | CVE-2015-8932 | not_reproducible_offline | `compress_bidder_init` invalid left-shift on crafted compress filter | Reproducer needs a `.Z`/compress-filter input with specific bit-width parameters; the validator does not ship the crafted compress fixture and a hand-rolled stream is rejected by the bidder before reaching the shift path. |
| libarchive | CVE-2015-8930 | not_reproducible_offline | ISO9660 directory cycle leading to bsdtar infinite loop | Crafted ISO image with a directory entry referring to itself; the validator has no canonical ISO fixture for this and constructing one offline is not feasible. |
| libarchive | CVE-2013-0211 | not_reproducible_offline | ZIP writer signed/unsigned size conversion on 64-bit hosts | Reproducer requires writing a single ZIP entry larger than 2 GiB so the on-disk size field hits the signedness boundary; the validator runs in CI sandboxes that cannot allocate or stream multi-gigabyte inputs deterministically. |
| libexif | CVE-2012-2837 | not_reproducible_offline | Divide-by-zero in `mnote_olympus_entry_get_value` while formatting Olympus MakerNote rationals | The bug is reachable only through Olympus-specific MakerNote tag layouts; the validator has no canonical Olympus MakerNote fixture and a hand-rolled MakerNote blob is rejected by the patched libexif before reaching the formatting path. |
| libexif | CVE-2020-13114 | not_reproducible_offline | Unrestricted Canon MakerNote size handling causing CPU exhaustion | Reproducer requires a structurally valid Canon MakerNote with attacker-controlled size fields; the validator has no canonical Canon MakerNote fixture and the patched libexif rejects naively-constructed MakerNotes upstream of the formerly-vulnerable accounting loop. |
| libpng | CVE-2007-5268 | not_reproducible_offline | Wrong operators/comparisons in palette/grayscale transform logic | Reproducer requires a palette-image-plus-specific-transform pipeline whose outputs differ between vulnerable and patched logic; the validator does not have canonical PNG/transform pairs that discriminate the patched predicate from the vulnerable one without rerunning libpng's own contrib test programs. |
| libpng | CVE-2009-2042 | not_reproducible_offline | Trailing-bit clearing for 1-bit interlaced rows whose width is not a byte multiple | This is an output-correctness regression: the discriminating signal is whether unused bits in the last packed row byte are zero. `pngfix`/`pngtest` do not surface raw decoded rows for byte-level comparison, and the matrix runner has no general PNG row-comparison fixture set. |
| libpng | CVE-2016-10087 | not_reproducible_offline | Text-chunk state-transition bug after `png_set_text` remove and re-add via the libpng write API | Reproducer requires exercising the libpng write API's text remove/replace cycle, which has no equivalent surface in `pngfix` / `pngtest` or any other CLI tool the validator ships; constructing a write-side regression that distinguishes vulnerable from patched control flow needs a custom C harness embedding the exact replace sequence, which is not available offline as a discriminating test. |
| libtiff | CVE-2006-0405 | not_reproducible_offline | `TIFFFetchShortPair` absent-data path during field setup | Reproducer requires a directory whose specific field offsets drive `TIFFFetchShortPair` past the absent-data check; the original PoC fixture is not part of the validator inputs and the bug is invisible from `tiffinfo` against generic malformed TIFFs since the patched library short-circuits earlier in the directory walk. |
| libtiff | CVE-2006-3464 | not_reproducible_offline | Large directory offsets bypassing range checks | Discriminating fixture requires a directory whose offsets straddle the formerly-unchecked arithmetic boundary in a way that survives basic format validation; constructing this offline without bit-perfect upstream fixtures is not deterministic. |
| libtiff | CVE-2010-2233 | not_reproducible_offline | OJPEG vertical-flip 64-bit image-state mishandling | The bug surfaces only inside the OJPEG codec when downsampling+flip apply; OJPEG decoder fixtures are not in the validator inputs and the patched library rejects malformed OJPEG headers before the formerly-vulnerable conversion runs. |
| libtiff | CVE-2010-2443 | not_reproducible_offline | `OJPEGReadBufferFill` absent-data path on undefined OJPEG strip offsets | Requires a crafted OJPEG fixture whose strip-offset metadata exercises the formerly-vulnerable absent-data branch; no canonical OJPEG fixture available offline. |
| libtiff | CVE-2010-2482 | not_reproducible_offline | `td_stripbytecount` absent-data handling in shared read paths | Discriminating fixture requires a directory whose `td_stripbytecount` is internally inconsistent in a way that bypasses the patched validation; offline construction is not deterministic. |
| libtiff | CVE-2010-2595 | not_reproducible_offline | `TIFFYCbCrtoRGB` accepted invalid `ReferenceBlackWhite` values | Reproducer needs a YCbCr image whose color-conversion pipeline is exercised end to end; `tiffinfo`/`tiffsplit` do not surface the conversion math on a malformed `ReferenceBlackWhite` and the validator has no canonical YCbCr fixture for this CVE. |
| libtiff | CVE-2010-2596 | not_reproducible_offline | OJPEGPostDecode reachable assertion on malformed OJPEG input | OJPEG codec fixtures with the specific assertion-triggering scan layout are not part of the validator inputs and cannot be reliably hand-crafted offline. |
| libtiff | CVE-2010-2597 | not_reproducible_offline | `TIFFVStripSize` left invalid state via incorrect `TIFFGetField` use | Reproducer depends on a directory whose strip-size accounting state is internally inconsistent in the formerly-vulnerable way; the patched library validates earlier and there is no offline-buildable input that distinguishes the patched control flow. |
| libtiff | CVE-2010-2598 | not_reproducible_offline | Image processing continued without required compression configuration | Bug surfaces when libtiff is built without specific codec features; on noble's stock build the codec is configured, so the same input parses identically to a patched build. |
| libtiff | CVE-2010-2630 | not_reproducible_offline | Codec-specific tags type-checked incorrectly when out of order | Reproducer needs codec-specific tag ordering that survives the patched directory parser; constructing a discriminating fixture offline is not deterministic. |
| libtiff | CVE-2010-2631 | not_reproducible_offline | First-stage parse leaving inconsistent state for second stage | Discriminating fixture requires a directory whose first-pass / second-pass handlers diverge on noble's libtiff in the way the vulnerable build did; offline construction is not deterministic. |
| libtiff | CVE-2015-7313 | not_reproducible_offline | Crafted TIFF input could trigger uncontrolled memory consumption before crashing | The patched libtiff bounds dimension-derived allocations, but the original PoC fixture is not part of the validator inputs and a generic large-dimension TIFF is already covered by CVE-2017-11613 / CVE-2023-6277 above. |
| libtiff | CVE-2016-3622 | not_reproducible_offline | `fpAcc` predictor accumulation divide-by-zero on crafted image data | Bug fires inside the floating-point predictor on a crafted strip; the validator has no canonical predictor-encoded TIFF fixture and a hand-rolled predictor stream is rejected before the formerly-vulnerable accumulator runs. |
| libtiff | CVE-2016-5323 | not_reproducible_offline | `_TIFFFax3fillruns` divide-by-zero on crafted image data | Requires a Fax-encoded TIFF whose run-length data drives the formerly-vulnerable divisor to zero; the validator has no canonical Fax fixture and naively-constructed Fax streams are rejected upstream. |
| libtiff | CVE-2016-9448 | not_reproducible_offline | `TIFFFetchNormalTag` absent-data path on zero-byte ASCII tags | Discriminating fixture requires an ASCII tag whose declared count is zero in a way that survives the patched validation; offline construction is not deterministic against noble's libtiff. |
| libtiff | CVE-2016-10266 | not_reproducible_offline | `tif_read.c` divide-by-zero on crafted TIFF data | Reproducer depends on a strip layout whose derived divisor zeroes inside the patched read logic; offline construction without canonical fixtures is not deterministic. |
| libtiff | CVE-2016-10267 | not_reproducible_offline | OJPEG divide-by-zero in `tif_ojpeg.c` | OJPEG fixture required; not in validator inputs and not buildable offline. |
| libtiff | CVE-2016-10371 | not_reproducible_offline | `TIFFWriteDirectoryTagCheckedRational` reachable assertion | Bug surfaces only when writing a directory with attacker-controlled rational tags; `tiffinfo`/`tiffsplit` do not exercise the write path and a directory-writing harness for this specific assertion is not available offline. |
| libtiff | CVE-2017-7592 | not_reproducible_offline | `putagreytile` left-shift undefined behaviour | Requires a grayscale tile whose pixel arithmetic exercises the formerly-vulnerable shift width; the patched libtiff masks the operand explicitly and the discriminating signal (UBSAN report) is not visible in normal `tiffinfo` runs. |
| libtiff | CVE-2017-7594 | not_reproducible_offline | `OJPEGReadHeaderInfoSecTablesDcTable` memory leak | Memory-leak regressions are invisible to the matrix runner, which records only exit status and bounded runtime; OJPEG fixture also unavailable. |
| libtiff | CVE-2017-7595 | not_reproducible_offline | `JPEGSetupEncode` divide-by-zero on invalid divisors | Bug fires only when libtiff is invoked as a JPEG encoder against attacker-controlled image state; the validator does not exercise the encode path with adversarial inputs and the patched library rejects naively-constructed JPEG-encoded directories upstream. |
| libtiff | CVE-2017-7596 | not_reproducible_offline | Floating-point conversion exceeded representable float range | Discriminating fixture requires a TIFF whose floating-point conversion path actually runs on noble; without a canonical PoC the patched library either rejects the input upstream or coerces silently, neither of which yields a stable signal. |
| libtiff | CVE-2017-7597 | not_reproducible_offline | `tif_dirread.c` float coercion outside representable range | Same constraint as CVE-2017-7596: no canonical fixture and the patched float coercion paths do not surface in `tiffinfo`. |
| libtiff | CVE-2017-7598 | not_reproducible_offline | `tif_dirread.c` divide-by-zero on crafted directory values | Bug class is covered structurally by CVE-2014-8130 / CVE-2004-0804 above; without a canonical PoC the specific divisor path in `tif_dirread.c` cannot be discriminated from the general directory-reading divide-by-zero set. |
| libtiff | CVE-2017-7599 | not_reproducible_offline | Conversion outside representable range of `short` | Same constraint as the other 2017 representability CVEs: no canonical fixture and the patched coercion is invisible to `tiffinfo`. |
| libtiff | CVE-2017-7600 | not_reproducible_offline | Conversion outside representable range of `unsigned char` | Same constraint as CVE-2017-7599. |
| libtiff | CVE-2017-7601 | not_reproducible_offline | Shift exponent beyond width of 64-bit `long` | Same constraint as CVE-2017-7592: no canonical fixture, signal is UBSAN-only on noble. |
| libtiff | CVE-2017-7602 | not_reproducible_offline | Signed integer overflow in shared arithmetic paths | Bug class is covered structurally by CVE-2019-14973 above; without a canonical PoC the specific arithmetic paths cannot be discriminated. |
| libtiff | CVE-2017-9403 | not_reproducible_offline | `TIFFReadDirEntryLong8Array` memory leak | Memory-leak regressions are invisible to the matrix runner. |
| libtiff | CVE-2017-9404 | not_reproducible_offline | `OJPEGReadHeaderInfoSecTablesQTable` memory leak | Memory-leak regression on OJPEG path; both regression-class and fixture are unavailable offline. |
| libtiff | CVE-2017-9815 | not_reproducible_offline | Long-array directory entry parse memory leak | Memory-leak regressions are invisible to the matrix runner. |
| libtiff | CVE-2017-9936 | not_reproducible_offline | JBIG decoder memory leak | JBIG fixture not in validator inputs and memory-leak signal not captured. |
| libtiff | CVE-2017-9937 | not_reproducible_offline | JBIG decoding aborted on allocation failure | JBIG fixture unavailable. |
| libtiff | CVE-2017-10688 | not_reproducible_offline | `TIFFWriteDirectoryTagCheckedLong8Array` reachable assertion | Bug surfaces only on the directory-writing path with crafted long8 arrays; `tiffinfo`/`tiffsplit` do not exercise this surface and no offline write harness is available. |
| libtiff | CVE-2017-12944 | not_reproducible_offline | `TIFFReadDirEntryArray` allocation-failure crash path | Discriminating fixture requires a short-file directory crafted to drive the formerly-vulnerable allocation-failure branch; offline construction is not deterministic. |
| libtiff | CVE-2017-13726 | not_reproducible_offline | SubIFD `TIFFWriteDirectorySec` reachable assertion | Write-path SubIFD bug not exercised by `tiffinfo`/`tiffsplit`. |
| libtiff | CVE-2017-13727 | not_reproducible_offline | `TIFFWriteDirectoryTagSubifd` reachable assertion | Write-path SubIFD-tag bug not exercised by `tiffinfo`/`tiffsplit`. |
| libtiff | CVE-2017-16232 | not_reproducible_offline | Reported leaks in `tif_open.c` / `tif_lzw.c` / `tif_aux.c` | Memory-leak regression set; the original report itself flags reproduction issues. |
| libtiff | CVE-2017-18013 | not_reproducible_offline | `TIFFPrintDirectory` absent-state dereference during metadata printing | The patched `tiffinfo` no longer enters the formerly-vulnerable branch on naively malformed TIFFs; without a canonical fixture the discriminating signal is not available offline. |
| libtiff | CVE-2018-7456 | not_reproducible_offline | Earlier `TIFFPrintDirectory` absent-metadata dereference | Same constraint as CVE-2017-18013. |
| libtiff | CVE-2018-10801 | not_reproducible_offline | `TIFFClientOpen` allocation leak on malformed setup | Memory-leak regression invisible to the matrix runner. |
| libtiff | CVE-2018-10963 | not_reproducible_offline | `TIFFWriteDirectorySec` reachable assertion (distinct from earlier CVEs) | Write-path bug not exercised by `tiffinfo`/`tiffsplit`. |
| libtiff | CVE-2018-17000 | not_reproducible_offline | `TIFFWriteDirectoryTagTransferfunction` absent-data comparison | Write-path transfer-function bug not exercised by `tiffinfo`/`tiffsplit`. |
| libtiff | CVE-2018-18661 | not_reproducible_offline | LZWDecode absent-data path | Requires an LZW-encoded TIFF whose internal state drives the formerly-vulnerable absent-data branch; no canonical LZW fixture available. |
| libtiff | CVE-2018-19210 | not_reproducible_offline | Separate `TIFFWriteDirectorySec` absent-data crash path | Write-path bug not exercised by `tiffinfo`/`tiffsplit`. |
| libtiff | CVE-2019-6128 | not_reproducible_offline | `TIFFFdOpen` allocation leak | Memory-leak regression invisible to the matrix runner. |
| libtiff | CVE-2019-7663 | not_reproducible_offline | `TIFFWriteDirectoryTagTransferfunction` invalid-address dereference | Write-path bug not exercised by `tiffinfo`/`tiffsplit`. |
| libtiff | CVE-2020-35521 | not_reproducible_offline | `tif_read.c` allocation-failure abort | Discriminating signal is process-abort under simulated allocation failure; the validator does not inject allocation failures. |
| libtiff | CVE-2020-35522 | not_reproducible_offline | PixarLog allocation-failure abort | PixarLog fixture not in validator inputs. |
| libtiff | CVE-2022-0561 | not_reproducible_offline | `TIFFFetchStripThing` absent-data into `memcpy` | Discriminating fixture requires a directory whose strip-fetch path lands on the formerly-vulnerable branch; offline construction is not deterministic against noble. |
| libtiff | CVE-2022-0562 | not_reproducible_offline | `TIFFReadDirectory` absent-directory data into `memcpy` | Same constraint as CVE-2022-0561. |
| libtiff | CVE-2022-0908 | not_reproducible_offline | `TIFFFetchNormalTag` absent-tag data into `memcpy` | Same constraint as CVE-2022-0561. |
| libtiff | CVE-2022-34266 | not_reproducible_offline | `TIFFFetchStripThing` proceeded with invalid range / uninitialized state | Same constraint as CVE-2022-0561. |
| libtiff | CVE-2023-2731 | not_reproducible_offline | LZWDecode null-state dereference | LZW fixture required; not in validator inputs. |
| libtiff | CVE-2023-2908 | not_reproducible_offline | `tif_dir.c` directory-field absent-state dereference | Discriminating fixture not deterministic offline. |
| libtiff | CVE-2024-7006 | not_reproducible_offline | `tif_dirinfo.c` allocation-failure absent-data dereference | Discriminating signal is process-abort under simulated allocation failure; validator does not inject. |
| libtiff | CVE-2025-61143 | not_reproducible_offline | `tif_open.c` absent-data dereference during file-open setup | Discriminating fixture for the patched open/setup branch not deterministic offline. |
| libvips | CVE-2018-7998 | not_reproducible_offline | NULL function-pointer race in `vips_region_generate` between failed delayed load and worker threads | Reproducer requires concurrent worker threads observing a delayed-load failure mid-stream; the validator runs single-threaded CLI invocations and cannot deterministically reproduce the multi-thread race window. |
| libvips | CVE-2019-6976 | not_reproducible_offline | `iofuncs/memory.c` did not zero allocated memory, leaking process bytes through corrupted-input output images | Discriminating signal is the byte-level content of output images on corrupt input — the matrix runner does not perform byte-content comparison against an expected zeroed-output baseline, and no canonical corrupt-input fixture for this CVE ships with the validator. |
