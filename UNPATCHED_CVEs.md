# Unpatched CVEs against Ubuntu 24.04 noble

This file tracks CVEs surfaced by safelibs port `relevant_cves.json`
files that were excluded from the regression testcase set because the
Ubuntu 24.04 noble baseline still exhibits the vulnerable behavior.
Each row records the library, CVE id, upstream evidence, and the
observation that prevented the testcase from passing on `original`.

| Library | CVE | Reason | Upstream evidence | Observation |
| --- | --- | --- | --- | --- |
| libjson | CVE-2013-6371 | not_reproducible_offline | json-c hash collision DoS fixed in 0.12 by salting string hashing with a random seed | No deterministic offline reproducer available — exercising the worst-case bucket distribution requires knowing the upstream hash function and crafting collisions that generalize across noble's libjson-c5 package; treated as covered structurally rather than as an executable regression. |
| libjansson | CVE-2013-6401 | not_reproducible_offline | Jansson 2.6 changed hash function and seeded hashes with random data | Hash-flooding regression cannot be encoded as a deterministic offline test against a randomized hasher; observable behavior on a single input is identical between vulnerable and patched builds. |
| libuv | CVE-2014-9748 | not_reproducible_offline | uv_rwlock_t fallback affected only Windows XP / Server 2003 | Linux harness does not exercise the Windows fallback rwlock path; the noble libuv1t64 build does not ship the affected code. |
| libuv | CVE-2015-0278 | not_reproducible_offline | Privilege drop in uv_spawn did not clear supplementary groups | A meaningful reproducer requires running as root, calling setgroups, and observing supplementary-group leakage in the spawned child; the validator harness does not have privileged container capabilities. |
| libuv | CVE-2024-22017 | not_reproducible_offline | setuid did not affect libuv's pre-initialized io_uring submissions | Demonstrating retained privileges across setuid requires an io_uring-capable kernel and a privileged-to-unprivileged transition that the noble container harness does not provide. |
| libxml | CVE-2008-4409 | not_reproducible_offline | Crafted predefined-entity definitions could exhaust memory | Public reproducers rely on internals of older libxml2 entity-table allocation; modern noble libxml2 has rewritten this path and there is no published canonical input that asserts patched behavior offline. |
| libxml | CVE-2012-0841 | not_reproducible_offline | Dictionary hash randomization mitigates collision DoS | Cannot construct deterministic adversarial keys against a randomized hash without knowledge of the per-process seed. |
| libxml | CVE-2013-0338 | not_reproducible_offline | Internal entity expansion linear-complexity DoS | The bug surfaces as wall-clock degradation on a vulnerable build; on a patched build the input parses identically to other billion-laughs variants, so a discriminating offline reproducer collapses into CVE-2003-1564 / CVE-2014-3660 rather than this CVE specifically. |
| libxml | CVE-2015-1819 | not_reproducible_offline | xmlReader streaming API failed constant-memory guarantee under entity expansion | Demonstrating the constant-memory regression requires measuring RSS over a long-running reader stream, which the matrix runner does not capture; the input itself is covered by the entity-expansion regressions. |
| libxml | CVE-2015-5312 | not_reproducible_offline | xmlStringLenDecodeEntities expansion-limit gap | Bypass converged with the general billion-laughs limits in upstream — same payloads now uniformly trigger the patched limit, so this CVE is not separable from CVE-2014-3660 in an offline test. |
| libxml | CVE-2015-8035 | not_reproducible_offline | XZ decompression error-handling loop did not terminate | Reproducer requires a crafted XZ-compressed XML whose error path drove the loop; the validator does not ship the malformed-xz fixture and constructing one offline that distinguishes vulnerable from patched behavior is not deterministic. |
| libxml | CVE-2016-4449 | not_reproducible_offline | Entity decoding outside validating mode fetched external content | Public reproducers depend on a network endpoint or a non-default loader callback; default noble configuration is covered by the XXE regressions for CVE-2013-0339 and CVE-2017-7375 and adds no separable signal for this CVE specifically. |
| libxml | CVE-2016-9318 | not_reproducible_offline | API design: lacked a same-document-only resolution policy | This is an API-surface concern: there is no input that distinguishes vulnerable from patched behavior at the xmllint command level; the patched API options exist but exercising them does not assert past-CVE state. |
| libxml | CVE-2017-18258 | not_reproducible_offline | LZMA decompression had no memory limit | Reproducer requires a crafted XZ-wrapped XML hitting xmlIO LZMA paths with attacker-driven memory allocation; no canonical fixture is shipped with the validator and constructing a discriminating one offline is not deterministic. |
| libxml | CVE-2018-9251 | not_reproducible_offline | LZMA memlimit-error infinite loop in libxml2 IO | Same shape as CVE-2017-18258 — distinguishing patched from vulnerable behavior requires an XZ fixture crafted to trigger the memlimit error path, which is not part of the validator fixture set. |
| libxml | CVE-2018-14567 | not_reproducible_offline | Second LZMA infinite-loop variant in the same code path | Variant of CVE-2018-9251 against the same crafted-XZ fixture; sharing the same offline reproducer constraint. |
| libxml | CVE-2021-3537 | not_reproducible_offline | Mixed-content parser-error propagation | Bug fired only when a specific schema-validation pipeline observed an error after a non-fatal parse; the public reproducer relies on internal reset paths that the patched libxml2 has refactored and no canonical input asserts the patched control flow offline. |
| libxml | CVE-2023-28484 | not_reproducible_offline | xmlSchemaFixupComplexType nullability bug | Public reproducer depends on a specific malformed XSD whose canonical form is not part of the validator fixture set; a discriminating offline input could not be constructed without risking false negatives on noble. |
| libxml | CVE-2025-27113 | not_reproducible_offline | xmlPatMatch nullability crash | The CVE record gives no concrete reproducer input; the patch added defensive null checks but no canonical malformed input is published, so an offline regression cannot distinguish vulnerable from patched builds. |
| libxml | CVE-2025-49795 | not_reproducible_offline | XPath null dereference on crafted expressions | Public reports describe the bug class but do not publish a deterministic input; XPath fuzz reproducers vary across libxml2 minor versions. |
| libxml | CVE-2026-0992 | not_reproducible_offline | Repeated nextCatalog traversal CPU exhaustion | The CPU-exhaustion regression requires measuring wall-clock degradation over many catalog hops; on a patched build the same input parses quickly, so the offline test collapses into CVE-2026-0990 rather than asserting this CVE separately. |
| liblzma | CVE-2024-3094 | not_reproducible_offline | Embedded malicious code in 5.6.0/5.6.1 release tarballs altered liblzma during the build | Bug class is supply-chain compromise of the release pipeline rather than a runtime parser path. Noble shipped 5.6.1+really5.4.5 (the revert), so the affected object code is absent and no input distinguishes vulnerable from patched at the library API level. |
| libbz2 | CVE-2005-1260 | not_reproducible_offline | Decompression-bomb / infinite-loop handling in BZ2_decompress before 1.0.3 | The historical PoC payload that drove the infinite loop is not part of the validator fixture set, and a hand-rolled corrupted bz2 stream is rejected identically by both the vulnerable and the patched library at the user-visible level — only wall-clock divergence distinguishes them, which the matrix runner does not measure. |
| libbz2 | CVE-2010-0405 | not_reproducible_offline | Integer overflow in BZ2_decompress selector / Huffman accounting before 1.0.6 | The exact bit-level fixture that exercises the unchecked nSelectors arithmetic is not published, and a generic corrupted bz2 stream is rejected by both vulnerable and patched libraries with identical user-visible diagnostics; constructing a discriminating offline reproducer would require bit-perfect reconstruction of the Huffman / selector layout. |
| libarchive | CVE-2026-5121 | not_reproducible_offline | 32-bit zisofs block-pointer allocation overflow in the ISO9660 reader | The allocation-overflow path is gated on a 32-bit target; the noble validator runs amd64 containers where the multiplication does not overflow, so a discriminating offline reproducer would require a 32-bit harness which the validator does not provide. |
| libarchive | CVE-2026-4426 | not_reproducible_offline | zisofs `pz_log2_bs` validation in the ISO9660 Rock Ridge extension reader | Reproducer requires a crafted ISO9660 image with a specific Rock Ridge zisofs extension setting an out-of-range `pz_log2_bs`; binary ISO fixture is not part of the validator inputs and cannot be reconstructed reliably from the CVE record alone. |
| libarchive | CVE-2026-4424 | not_reproducible_offline | RAR LZSS sliding-window resize across compression-method transitions | The bug only surfaces inside the proprietary RAR decompressor on a multi-method archive; constructing a minimal RAR with the exact compression-method transition that re-resizes the window would require bit-level RAR encoder knowledge that the validator does not have. |
| libarchive | CVE-2026-4111 | not_reproducible_offline | RAR5 `archive_read_data()` infinite loop on no-progress decompression state | Reproducer needs a structurally valid (checksum-passing) RAR5 archive that drives the decompressor into a no-progress state; the validator has no canonical RAR5 fixture and bsdtar would short-circuit any hand-built malformed RAR5 before reaching the no-progress code path. |
| libarchive | CVE-2025-5915 | not_reproducible_offline | RAR filter-block size versus LZSS-window validation | Same constraint as the other RAR-decoder CVEs — a discriminating fixture must be a valid RAR with a precisely-sized filter block exceeding the active LZSS window, and that input cannot be hand-rolled offline. |
| libarchive | CVE-2025-5914 | not_reproducible_offline | RAR `archive_read_format_rar_seek_data()` integer overflow | Crafted RAR seek-position fixture is not in the validator inputs; without an authentic RAR encoder the seek arithmetic cannot be exercised. |
| libarchive | CVE-2024-48958 | not_reproducible_offline | RAR delta-filter `src` cursor advancing beyond `dst` | Requires a crafted RAR with the filter metadata configured to drive cursor movement past the destination window; binary RAR construction is not feasible from the CVE record. |
| libarchive | CVE-2024-48957 | not_reproducible_offline | RAR audio-filter `src` cursor advancing beyond `dst` | Same RAR-encoder constraint as the delta-filter case. |
| libarchive | CVE-2023-30571 | not_reproducible_offline | `umask()` race in `archive_write_disk_posix.c` flipping process-wide umask to 0 | Bug class is a thread-race that requires a multithreaded extraction harness with one thread reading the global umask while another mutates it; the validator does not run libarchive in a multithreaded context. |
| libarchive | CVE-2021-31566 | not_reproducible_offline | Improper symlink-target resolution while applying metadata during extraction | Reproducer relies on a symlink that points outside the extraction root and on metadata operations that follow that link; constructing a deterministic offline test requires either privileged container capabilities or a filesystem mount that escapes the harness, neither of which the validator provides. |
| libarchive | CVE-2021-23177 | not_reproducible_offline | Improper link resolution during ACL application | Same containment-test setup as CVE-2021-31566, plus the additional dependency on POSIX ACL support; the validator does not exercise the ACL extraction path under controlled symlink conditions. |
| libarchive | CVE-2020-9308 | not_reproducible_offline | RAR5 zero-size header dispatch in `archive_read_support_format_rar5.c` | Hand-rolled RAR5 magic + zero-length header is accepted as an empty archive by the patched bsdtar (rc=0, no entries) and therefore does not exercise the formerly-vulnerable header-dispatch path; a real PoC needs a structurally valid RAR5 wrapper around the malformed header. |
| libarchive | CVE-2019-1000020 | not_reproducible_offline | ISO9660 Rock Ridge continuation infinite loop in `read_CE()` / `parse_rockridge()` | Reproducer is a binary ISO9660 image with a self-referential Rock Ridge continuation chain; the validator does not ship the fixture and constructing one bit-perfectly is not feasible. |
| libarchive | CVE-2016-8689 | not_reproducible_offline | 7zip `read_Header` mishandling of repeated EmptyStream attributes | 7zip header-dispatch fixture cannot be hand-rolled; the validator has no canonical malformed 7z input. |
| libarchive | CVE-2016-6250 | not_reproducible_offline | ISO9660 writer integer overflow in filename-length verification | The malformed-input branch is reachable only when an extraordinarily long filename is fed through `bsdtar --format iso9660`; on noble libarchive 3.7.2 the writer succeeds for hand-rolled long names without surfacing the bounds-validation diagnostic, so an offline test cannot distinguish the patched arithmetic from the vulnerable path. |
| libarchive | CVE-2016-5844 | not_reproducible_offline | ISO parser integer overflow in offset / length math | Crafted ISO9660 fixture with overflowing offset fields is not part of the validator inputs and cannot be hand-rolled with reasonable confidence. |
| libarchive | CVE-2016-4300 | not_reproducible_offline | 7zip `read_SubStreamsInfo` integer overflow on large substream count | 7zip header construction beyond the validator's tooling; no canonical fixture available. |
| libarchive | CVE-2015-8932 | not_reproducible_offline | `compress_bidder_init` invalid left-shift on crafted compress filter | Reproducer needs a `.Z`/compress-filter input with specific bit-width parameters; the validator does not ship the crafted compress fixture and a hand-rolled stream is rejected by the bidder before reaching the shift path. |
| libarchive | CVE-2015-8930 | not_reproducible_offline | ISO9660 directory cycle leading to bsdtar infinite loop | Crafted ISO image with a directory entry referring to itself; the validator has no canonical ISO fixture for this and constructing one offline is not feasible. |
| libarchive | CVE-2013-0211 | not_reproducible_offline | ZIP writer signed/unsigned size conversion on 64-bit hosts | Reproducer requires writing a single ZIP entry larger than 2 GiB so the on-disk size field hits the signedness boundary; the validator runs in CI sandboxes that cannot allocate or stream multi-gigabyte inputs deterministically. |
| libexif | CVE-2012-2837 | not_reproducible_offline | Divide-by-zero in `mnote_olympus_entry_get_value` while formatting Olympus MakerNote rationals | The bug is reachable only through Olympus-specific MakerNote tag layouts; the validator has no canonical Olympus MakerNote fixture and a hand-rolled MakerNote blob is rejected by the patched libexif before reaching the formatting path. |
| libexif | CVE-2020-13114 | not_reproducible_offline | Unrestricted Canon MakerNote size handling causing CPU exhaustion | Reproducer requires a structurally valid Canon MakerNote with attacker-controlled size fields; the validator has no canonical Canon MakerNote fixture and the patched libexif rejects naively-constructed MakerNotes upstream of the formerly-vulnerable accounting loop. |
| libpng | CVE-2007-5268 | not_reproducible_offline | Wrong operators/comparisons in palette/grayscale transform logic | Reproducer requires a palette-image-plus-specific-transform pipeline whose outputs differ between vulnerable and patched logic; the validator does not have canonical PNG/transform pairs that discriminate the patched predicate from the vulnerable one without rerunning libpng's own contrib test programs. |
| libpng | CVE-2009-2042 | not_reproducible_offline | Trailing-bit clearing for 1-bit interlaced rows whose width is not a byte multiple | This is an output-correctness regression: the discriminating signal is whether unused bits in the last packed row byte are zero. `pngfix`/`pngtest` do not surface raw decoded rows for byte-level comparison, and the matrix runner has no general PNG row-comparison fixture set. |
| libpng | CVE-2016-10087 | not_reproducible_offline | Text-chunk state-transition bug after `png_set_text` remove and re-add via the libpng write API | Reproducer requires exercising the libpng write API's text remove/replace cycle, which has no equivalent surface in `pngfix` / `pngtest` or any other CLI tool the validator ships; constructing a write-side regression that distinguishes vulnerable from patched control flow needs a custom C harness embedding the exact replace sequence, which is not available offline as a discriminating test. |
| libtiff | CVE-2006-0405 | not_reproducible_offline | `TIFFFetchShortPair` absent-data path during field setup | Reproducer requires a directory whose specific field offsets drive `TIFFFetchShortPair` past the absent-data check; the original PoC fixture is not part of the validator inputs and the bug is invisible from `tiffinfo` against generic malformed TIFFs since the patched library short-circuits earlier in the directory walk. |
| libtiff | CVE-2006-3464 | not_reproducible_offline | Large directory offsets bypassing range checks | Discriminating fixture requires a directory whose offsets straddle the formerly-unchecked arithmetic boundary in a way that survives basic format validation; constructing this offline without bit-perfect upstream fixtures is not deterministic. |
| libtiff | CVE-2010-2233 | not_reproducible_offline | OJPEG vertical-flip 64-bit image-state mishandling | The bug surfaces only inside the OJPEG codec when downsampling+flip apply; OJPEG decoder fixtures are not in the validator inputs and the patched library rejects malformed OJPEG headers before the formerly-vulnerable conversion runs. |
| libtiff | CVE-2010-2443 | not_reproducible_offline | `OJPEGReadBufferFill` absent-data path on undefined OJPEG strip offsets | Requires a crafted OJPEG fixture whose strip-offset metadata exercises the formerly-vulnerable absent-data branch; no canonical OJPEG fixture available offline. |
| libtiff | CVE-2010-2482 | not_reproducible_offline | `td_stripbytecount` absent-data handling in shared read paths | Discriminating fixture requires a directory whose `td_stripbytecount` is internally inconsistent in a way that bypasses the patched validation; offline construction is not deterministic. |
| libtiff | CVE-2010-2595 | not_reproducible_offline | `TIFFYCbCrtoRGB` accepted invalid `ReferenceBlackWhite` values | Reproducer needs a YCbCr image whose color-conversion pipeline is exercised end to end; `tiffinfo`/`tiffsplit` do not surface the conversion math on a malformed `ReferenceBlackWhite` and the validator has no canonical YCbCr fixture for this CVE. |
| libtiff | CVE-2010-2596 | not_reproducible_offline | OJPEGPostDecode reachable assertion on malformed OJPEG input | OJPEG codec fixtures with the specific assertion-triggering scan layout are not part of the validator inputs and cannot be reliably hand-crafted offline. |
| libtiff | CVE-2010-2597 | not_reproducible_offline | `TIFFVStripSize` left invalid state via incorrect `TIFFGetField` use | Reproducer depends on a directory whose strip-size accounting state is internally inconsistent in the formerly-vulnerable way; the patched library validates earlier and there is no offline-buildable input that distinguishes the patched control flow. |
| libtiff | CVE-2010-2598 | not_reproducible_offline | Image processing continued without required compression configuration | Bug surfaces when libtiff is built without specific codec features; on noble's stock build the codec is configured, so the same input parses identically to a patched build. |
| libtiff | CVE-2010-2630 | not_reproducible_offline | Codec-specific tags type-checked incorrectly when out of order | Reproducer needs codec-specific tag ordering that survives the patched directory parser; constructing a discriminating fixture offline is not deterministic. |
| libtiff | CVE-2010-2631 | not_reproducible_offline | First-stage parse leaving inconsistent state for second stage | Discriminating fixture requires a directory whose first-pass / second-pass handlers diverge on noble's libtiff in the way the vulnerable build did; offline construction is not deterministic. |
| libtiff | CVE-2015-7313 | not_reproducible_offline | Crafted TIFF input could trigger uncontrolled memory consumption before crashing | The patched libtiff bounds dimension-derived allocations, but the original PoC fixture is not part of the validator inputs and a generic large-dimension TIFF is already covered by CVE-2017-11613 / CVE-2023-6277 above. |
| libtiff | CVE-2016-3622 | not_reproducible_offline | `fpAcc` predictor accumulation divide-by-zero on crafted image data | Bug fires inside the floating-point predictor on a crafted strip; the validator has no canonical predictor-encoded TIFF fixture and a hand-rolled predictor stream is rejected before the formerly-vulnerable accumulator runs. |
| libtiff | CVE-2016-5323 | not_reproducible_offline | `_TIFFFax3fillruns` divide-by-zero on crafted image data | Requires a Fax-encoded TIFF whose run-length data drives the formerly-vulnerable divisor to zero; the validator has no canonical Fax fixture and naively-constructed Fax streams are rejected upstream. |
| libtiff | CVE-2016-9448 | not_reproducible_offline | `TIFFFetchNormalTag` absent-data path on zero-byte ASCII tags | Discriminating fixture requires an ASCII tag whose declared count is zero in a way that survives the patched validation; offline construction is not deterministic against noble's libtiff. |
| libtiff | CVE-2016-10266 | not_reproducible_offline | `tif_read.c` divide-by-zero on crafted TIFF data | Reproducer depends on a strip layout whose derived divisor zeroes inside the patched read logic; offline construction without canonical fixtures is not deterministic. |
| libtiff | CVE-2016-10267 | not_reproducible_offline | OJPEG divide-by-zero in `tif_ojpeg.c` | OJPEG fixture required; not in validator inputs and not buildable offline. |
| libtiff | CVE-2016-10371 | not_reproducible_offline | `TIFFWriteDirectoryTagCheckedRational` reachable assertion | Bug surfaces only when writing a directory with attacker-controlled rational tags; `tiffinfo`/`tiffsplit` do not exercise the write path and a directory-writing harness for this specific assertion is not available offline. |
| libtiff | CVE-2017-7592 | not_reproducible_offline | `putagreytile` left-shift undefined behaviour | Requires a grayscale tile whose pixel arithmetic exercises the formerly-vulnerable shift width; the patched libtiff masks the operand explicitly and the discriminating signal (UBSAN report) is not visible in normal `tiffinfo` runs. |
| libtiff | CVE-2017-7594 | not_reproducible_offline | `OJPEGReadHeaderInfoSecTablesDcTable` memory leak | Memory-leak regressions are invisible to the matrix runner, which records only exit status and bounded runtime; OJPEG fixture also unavailable. |
| libtiff | CVE-2017-7595 | not_reproducible_offline | `JPEGSetupEncode` divide-by-zero on invalid divisors | Bug fires only when libtiff is invoked as a JPEG encoder against attacker-controlled image state; the validator does not exercise the encode path with adversarial inputs and the patched library rejects naively-constructed JPEG-encoded directories upstream. |
| libtiff | CVE-2017-7596 | not_reproducible_offline | Floating-point conversion exceeded representable float range | Discriminating fixture requires a TIFF whose floating-point conversion path actually runs on noble; without a canonical PoC the patched library either rejects the input upstream or coerces silently, neither of which yields a stable signal. |
| libtiff | CVE-2017-7597 | not_reproducible_offline | `tif_dirread.c` float coercion outside representable range | Same constraint as CVE-2017-7596: no canonical fixture and the patched float coercion paths do not surface in `tiffinfo`. |
| libtiff | CVE-2017-7598 | not_reproducible_offline | `tif_dirread.c` divide-by-zero on crafted directory values | Bug class is covered structurally by CVE-2014-8130 / CVE-2004-0804 above; without a canonical PoC the specific divisor path in `tif_dirread.c` cannot be discriminated from the general directory-reading divide-by-zero set. |
| libtiff | CVE-2017-7599 | not_reproducible_offline | Conversion outside representable range of `short` | Same constraint as the other 2017 representability CVEs: no canonical fixture and the patched coercion is invisible to `tiffinfo`. |
| libtiff | CVE-2017-7600 | not_reproducible_offline | Conversion outside representable range of `unsigned char` | Same constraint as CVE-2017-7599. |
| libtiff | CVE-2017-7601 | not_reproducible_offline | Shift exponent beyond width of 64-bit `long` | Same constraint as CVE-2017-7592: no canonical fixture, signal is UBSAN-only on noble. |
| libtiff | CVE-2017-7602 | not_reproducible_offline | Signed integer overflow in shared arithmetic paths | Bug class is covered structurally by CVE-2019-14973 above; without a canonical PoC the specific arithmetic paths cannot be discriminated. |
| libtiff | CVE-2017-9403 | not_reproducible_offline | `TIFFReadDirEntryLong8Array` memory leak | Memory-leak regressions are invisible to the matrix runner. |
| libtiff | CVE-2017-9404 | not_reproducible_offline | `OJPEGReadHeaderInfoSecTablesQTable` memory leak | Memory-leak regression on OJPEG path; both regression-class and fixture are unavailable offline. |
| libtiff | CVE-2017-9815 | not_reproducible_offline | Long-array directory entry parse memory leak | Memory-leak regressions are invisible to the matrix runner. |
| libtiff | CVE-2017-9936 | not_reproducible_offline | JBIG decoder memory leak | JBIG fixture not in validator inputs and memory-leak signal not captured. |
| libtiff | CVE-2017-9937 | not_reproducible_offline | JBIG decoding aborted on allocation failure | JBIG fixture unavailable. |
| libtiff | CVE-2017-10688 | not_reproducible_offline | `TIFFWriteDirectoryTagCheckedLong8Array` reachable assertion | Bug surfaces only on the directory-writing path with crafted long8 arrays; `tiffinfo`/`tiffsplit` do not exercise this surface and no offline write harness is available. |
| libtiff | CVE-2017-12944 | not_reproducible_offline | `TIFFReadDirEntryArray` allocation-failure crash path | Discriminating fixture requires a short-file directory crafted to drive the formerly-vulnerable allocation-failure branch; offline construction is not deterministic. |
| libtiff | CVE-2017-13726 | not_reproducible_offline | SubIFD `TIFFWriteDirectorySec` reachable assertion | Write-path SubIFD bug not exercised by `tiffinfo`/`tiffsplit`. |
| libtiff | CVE-2017-13727 | not_reproducible_offline | `TIFFWriteDirectoryTagSubifd` reachable assertion | Write-path SubIFD-tag bug not exercised by `tiffinfo`/`tiffsplit`. |
| libtiff | CVE-2017-16232 | not_reproducible_offline | Reported leaks in `tif_open.c` / `tif_lzw.c` / `tif_aux.c` | Memory-leak regression set; the original report itself flags reproduction issues. |
| libtiff | CVE-2017-18013 | not_reproducible_offline | `TIFFPrintDirectory` absent-state dereference during metadata printing | The patched `tiffinfo` no longer enters the formerly-vulnerable branch on naively malformed TIFFs; without a canonical fixture the discriminating signal is not available offline. |
| libtiff | CVE-2018-7456 | not_reproducible_offline | Earlier `TIFFPrintDirectory` absent-metadata dereference | Same constraint as CVE-2017-18013. |
| libtiff | CVE-2018-10801 | not_reproducible_offline | `TIFFClientOpen` allocation leak on malformed setup | Memory-leak regression invisible to the matrix runner. |
| libtiff | CVE-2018-10963 | not_reproducible_offline | `TIFFWriteDirectorySec` reachable assertion (distinct from earlier CVEs) | Write-path bug not exercised by `tiffinfo`/`tiffsplit`. |
| libtiff | CVE-2018-17000 | not_reproducible_offline | `TIFFWriteDirectoryTagTransferfunction` absent-data comparison | Write-path transfer-function bug not exercised by `tiffinfo`/`tiffsplit`. |
| libtiff | CVE-2018-18661 | not_reproducible_offline | LZWDecode absent-data path | Requires an LZW-encoded TIFF whose internal state drives the formerly-vulnerable absent-data branch; no canonical LZW fixture available. |
| libtiff | CVE-2018-19210 | not_reproducible_offline | Separate `TIFFWriteDirectorySec` absent-data crash path | Write-path bug not exercised by `tiffinfo`/`tiffsplit`. |
| libtiff | CVE-2019-6128 | not_reproducible_offline | `TIFFFdOpen` allocation leak | Memory-leak regression invisible to the matrix runner. |
| libtiff | CVE-2019-7663 | not_reproducible_offline | `TIFFWriteDirectoryTagTransferfunction` invalid-address dereference | Write-path bug not exercised by `tiffinfo`/`tiffsplit`. |
| libtiff | CVE-2020-35521 | not_reproducible_offline | `tif_read.c` allocation-failure abort | Discriminating signal is process-abort under simulated allocation failure; the validator does not inject allocation failures. |
| libtiff | CVE-2020-35522 | not_reproducible_offline | PixarLog allocation-failure abort | PixarLog fixture not in validator inputs. |
| libtiff | CVE-2022-0561 | not_reproducible_offline | `TIFFFetchStripThing` absent-data into `memcpy` | Discriminating fixture requires a directory whose strip-fetch path lands on the formerly-vulnerable branch; offline construction is not deterministic against noble. |
| libtiff | CVE-2022-0562 | not_reproducible_offline | `TIFFReadDirectory` absent-directory data into `memcpy` | Same constraint as CVE-2022-0561. |
| libtiff | CVE-2022-0908 | not_reproducible_offline | `TIFFFetchNormalTag` absent-tag data into `memcpy` | Same constraint as CVE-2022-0561. |
| libtiff | CVE-2022-34266 | not_reproducible_offline | `TIFFFetchStripThing` proceeded with invalid range / uninitialized state | Same constraint as CVE-2022-0561. |
| libtiff | CVE-2023-2731 | not_reproducible_offline | LZWDecode null-state dereference | LZW fixture required; not in validator inputs. |
| libtiff | CVE-2023-2908 | not_reproducible_offline | `tif_dir.c` directory-field absent-state dereference | Discriminating fixture not deterministic offline. |
| libtiff | CVE-2024-7006 | not_reproducible_offline | `tif_dirinfo.c` allocation-failure absent-data dereference | Discriminating signal is process-abort under simulated allocation failure; validator does not inject. |
| libtiff | CVE-2025-61143 | not_reproducible_offline | `tif_open.c` absent-data dereference during file-open setup | Discriminating fixture for the patched open/setup branch not deterministic offline. |
| libvips | CVE-2018-7998 | not_reproducible_offline | NULL function-pointer race in `vips_region_generate` between failed delayed load and worker threads | Reproducer requires concurrent worker threads observing a delayed-load failure mid-stream; the validator runs single-threaded CLI invocations and cannot deterministically reproduce the multi-thread race window. |
| libvips | CVE-2019-6976 | not_reproducible_offline | `iofuncs/memory.c` did not zero allocated memory, leaking process bytes through corrupted-input output images | Discriminating signal is the byte-level content of output images on corrupt input — the matrix runner does not perform byte-content comparison against an expected zeroed-output baseline, and no canonical corrupt-input fixture for this CVE ships with the validator. |
| glib | CVE-2009-3289 | not_reproducible_offline | g_file_copy preserved 0777 mode from a symlink target onto the destination file | The vulnerable surface depends on a symlink whose mode is broader than the running user's umask and on operating across user accounts; the validator runs single-user containers without privileged capabilities, so it cannot demonstrate cross-user mode leakage in a way that distinguishes patched from vulnerable behavior. |
| glib | CVE-2012-0039 | not_reproducible_offline | g_str_hash collision DoS against unsalted string hashing | Modern noble glib seeds and randomizes hash table behavior through GHashTable abstractions; constructing collision sets that distinguish vulnerable from patched behavior on a single input is not feasible offline because the disputed CVE describes a callable surface, not an input that triggers different runtime cost on noble. |
| glib | CVE-2019-12450 | not_reproducible_offline | file_copy_fallback exposed permissive default modes for the destination file mid-copy | The discriminating signal is the transient mode bits on the destination file during the copy window; observing those bits requires racing the helper from a second process under controlled umask conditions, which the single-shot matrix runner does not provide. |
| glib | CVE-2019-13012 | not_reproducible_offline | GSettings keyfile backend created configuration directories with 0777 and files with default permissions | Reproducing the bug requires running through the GSettings/dconf keyfile backend pipeline against a writable XDG config tree, which the validator does not expose; the discriminating signal is mode bits on backend-created files rather than a bounded input/output observation. |
| glib | CVE-2020-6750 | not_reproducible_offline | GSocketClient occasionally connected directly to the target instead of via the configured proxy | The bug is timing-dependent and surfaces only when the proxy connection times out before the direct attempt; reproducing it offline would require a controlled proxy server plus reliable timing manipulation, neither of which the validator harness provides. |
| glib | CVE-2021-3800 | not_reproducible_offline | pkexec/charset.alias environment-driven charset resolution leaked privileged file content to unprivileged callers | Reproducing the leak requires a setuid pkexec-style trust transition with attacker-controlled charset.alias overrides; the validator runs unprivileged single-user containers and cannot exercise the cross-privilege-boundary path. |
| glib | CVE-2021-27218 | not_reproducible_offline | g_byte_array_new_take with a >=4 GiB buffer truncated the length modulo 2**32 | Triggering the truncation requires allocating and passing a 4 GiB-or-larger contiguous buffer to the GLib API; the matrix runner does not provision that much RAM for a single testcase and a smaller buffer cannot exercise the wraparound. |
| glib | CVE-2023-32636 | not_reproducible_offline | GVariant offset-table validation became very slow after the CVE-2023-29499 backport in distributors | The bug only affects glib distributions that backported the original CVE-2023-29499 fix without the follow-up adjustment; noble glib carries the corrected patch set, so on this baseline the slow-validation path is structurally absent and any GVariant fuzz input collapses into the CVE-2023-29499 / CVE-2023-32611 regressions. |
| glib | CVE-2024-34397 | not_reproducible_offline | GDBus subscriptions accepted spoofed signals from untrusted local senders impersonating system services | Reproducing the spoof requires a session/system D-Bus daemon plus a second authenticated client masquerading as NetworkManager-class peer; the validator does not run a system bus and cannot exercise GDBus signal subscription dispatch end-to-end. |
| glib | CVE-2025-4056 | not_reproducible_offline | GLib's Windows g_spawn implementation could DoS on oversized command lines | Bug only fires inside the Win32 g_spawn_async family on Windows; noble glib runs through the POSIX spawn path, so no input distinguishes vulnerable from patched behavior on a Linux validator container. |
| libc6 | CVE-2026-4438 | not_reproducible_offline | gethostbyaddr[_r] could return an invalid DNS hostname violating the DNS specification | Same DNS-responder dependency as CVE-2026-4437; without an in-container authoritative resolver the validator cannot inject invalid label sequences into the gethostbyaddr return path. |
| libc6 | CVE-2026-4437 | not_reproducible_offline | Crafted reverse-DNS responses made gethostbyaddr[_r] treat a non-answer section as a valid answer | Reproducer requires a controlled DNS responder returning a crafted reverse-answer payload; the validator container does not host a configurable resolver, so the parser-selection branch cannot be driven offline. |
| libc6 | CVE-2026-3904 | not_reproducible_offline | libc-side nscd client compared concurrently modified shared cache data and could crash | Reproducer requires a running nscd daemon under contended cache mutation while a libc client reads through the shared-memory window; the matrix runner does not orchestrate nscd in a way that exposes this race. |
| libc6 | CVE-2025-0577 | not_reproducible_offline | Fork-concurrent calls to getrandom/arc4random returned predictable randomness because post-fork RNG state was not refreshed | Reproducing the prediction requires racing a fork with a pre-existing RNG state and observing repeated outputs across two children; the discriminating signal is statistical correlation across PIDs, which the matrix runner does not capture in a single bounded testcase. |
| libc6 | CVE-2025-4802 | not_reproducible_offline | Statically linked setuid binaries that use dlopen honored attacker-controlled LD_LIBRARY_PATH | Bug requires a statically linked setuid binary calling dlopen; the unprivileged validator container does not run setuid binaries and noble's matrix tests are dynamically linked, so the affected secure-exec branch is unreachable. |
| libc6 | CVE-2024-12455 | not_reproducible_offline | powerpc getrandom returned EINVAL as the function result instead of setting errno | Bug only manifests on powerpc; the noble validator runs amd64 containers where the affected machine-specific syscall stub is structurally absent. |
| libc6 | CVE-2016-20013 | not_reproducible_offline | SHA-crypt password hashing was quadratic in password length, enabling CPU-exhaustion attacks | The complexity is inherent to the SHA-crypt algorithm rather than to a specific glibc version; both vulnerable and patched libcs exhibit the same quadratic shape on a long password, so an offline reproducer cannot discriminate fixed from unfixed behavior. |
| libc6 | CVE-2021-3998 | not_reproducible_offline | realpath returned an unexpected value, disclosing path information | The historical PoC depends on a multi-step concurrent rename sequence whose timing the patched glibc closes; reproducing the disclosure offline requires racing realpath against rename in a way the single-shot matrix runner cannot orchestrate. |
| libc6 | CVE-2019-19126 | not_reproducible_offline | Secure execution on x86-64 failed to ignore LD_PREFER_MAP_32BIT_EXEC after a security transition | Discriminating signal is the secure-execution loader honoring an environment-driven mapping policy across a setuid transition; the validator does not run setuid binaries and cannot exercise the secure-exec branch. |
| libc6 | CVE-2018-20796 | not_reproducible_offline | regexec hit uncontrolled recursion on a crafted alternation pattern | On noble glibc the patched matcher still spends meaningful time on the historical recursion-pattern, so a deterministic 60-second offline reproducer cannot reliably discriminate vulnerable from patched behavior; the bug class converges with CVE-2009-5155 on noble. |
| libc6 | CVE-2019-7309 | not_reproducible_offline | x32 memcmp could incorrectly report equality due to register handling errors | Bug only manifests under the x32 ABI; noble validator containers run amd64 (LP64) and the affected x32 memcmp path is structurally absent. |
| libc6 | CVE-2018-19591 | not_reproducible_offline | Resolving a crafted hostname via getaddrinfo allocated a socket descriptor that was never closed | Discriminating signal is a long-running fd leak observed across many resolver invocations; the matrix runner records exit status only and does not capture per-process fd accounting needed to distinguish the leaking branch from the patched cleanup. |
| libc6 | CVE-2017-16997 | not_reproducible_offline | Empty RPATH/RUNPATH tokens during privileged loading were misread as the current working directory | Reproducer requires a setuid binary linked with an attacker-controlled empty-token RPATH plus a writable cwd; the unprivileged validator container does not run setuid binaries and cannot exercise the privileged loader path. |
| libc6 | CVE-2017-12132 | not_reproducible_offline | Stub resolver EDNS support solicited large UDP responses, easing off-path DNS spoofing via fragmentation | Reproducing the spoof opportunity requires a controlled DNS responder issuing fragmented UDP answers and a stub-resolver query against it; the validator does not run a DNS infrastructure and cannot exercise the EDNS amplification path. |
| libc6 | CVE-2015-8985 | not_reproducible_offline | Extended regular-expression processing hit an assertion in pop_fail_stack and crashed | The historical assertion-trigger pattern is rejected by the patched compiler before the formerly-vulnerable runtime branch executes, so the discriminating signal converges with CVE-2009-5155 on noble; an offline test cannot separate this CVE from the broader regex-parser hardening already covered. |
| libc6 | CVE-2016-6323 | not_reproducible_offline | ARM EABI makecontext produced contexts incompatible with unwinding, causing hangs | Bug only manifests on ARM EABI 32-bit; the noble validator runs amd64 containers where the affected makecontext path is structurally absent. |
| libc6 | CVE-2015-8777 | not_reproducible_offline | LD_POINTER_GUARD could disable pointer-guarding across privilege boundaries | Same secure-execution / setuid constraint as the other ld.so policy CVEs; the validator runs unprivileged binaries and cannot demonstrate the dropped hardening on a privileged transition. |
| libc6 | CVE-2014-8121 | not_reproducible_offline | NSS files iteration concurrent with a database lookup reset the file pointer and looped forever | Reproducer requires two concurrent threads holding the same NSS db handle in a shared-iteration plus lookup pattern; the matrix runner invokes single-threaded test binaries and cannot stage the cross-thread state-reset window. |
| libc6 | CVE-2014-9402 | not_reproducible_offline | A positive DNS answer during getnetbyname processing sent the call into an infinite loop | Reproducing the loop requires a controlled DNS responder returning a crafted positive answer for the network-name query; the validator does not provide an in-container DNS server and cannot drive the resolver into the formerly-vulnerable branch. |
| libc6 | CVE-2014-0475 | not_reproducible_offline | Locale environment variables containing .. could traverse directories and bypass restrictions | Reproducing the traversal requires a setuid trust transition where LC_* are re-honored across the boundary; the unprivileged validator container does not provide a setuid surface, and the patched glibc rejects the traversal before any user-visible diagnostic distinguishes it from a normal locale-not-found path. |
| libc6 | CVE-2013-4788 | not_reproducible_offline | Statically linked executables initialized PTR_MANGLE with an all-zero pointer guard | Discriminating signal is the value of the per-process pointer guard inside a statically linked binary; noble's libc6 ships dynamic glibc and the validator harness invokes dynamically linked test programs, so the static-only initialization path is not reachable. |
| libc6 | CVE-2011-4609 | not_reproducible_offline | sunrpc svc_run consumed excessive CPU under large numbers of RPC connections | Reproducer requires standing up a sunrpc server and driving it with many concurrent connections; the matrix runner does not orchestrate multi-process RPC fan-in inside a single testcase. |
| libc6 | CVE-2011-1089 | not_reproducible_offline | addmntent silently ignored write failures, masking /etc/mtab corruption | Reproducer requires writing to a privileged mtab path under simulated I/O failure; the validator cannot inject write errors against /etc/mtab without root and the failure-suppression path is invisible at the user-space CLI level. |
| libc6 | CVE-2011-1658 | not_reproducible_offline | $ORIGIN-only RPATH values were expanded unsafely inside privileged programs | The discriminating surface is the dynamic linker's secure-execution mode plus a setuid program with a vulnerable RPATH; the unprivileged validator container does not run setuid binaries and cannot exercise the privileged token-expansion branch. |
| libc6 | CVE-2010-4756 | not_reproducible_offline | Crafted unmatched glob expressions consumed excessive CPU and memory in glob() | The bug class is wall-clock divergence on adversarial glob inputs; on noble glibc the patched limits make the offline reproducer either succeed quickly or hit the 60-second matrix timeout regardless of branch, so no bounded test discriminates the two builds. |
| libc6 | CVE-2010-4052 | not_reproducible_offline | Adjacent repetition operators caused unbounded stack consumption in regcomp | Discriminating signal is process-level stack exhaustion versus bounded compile time; the patched glibc still consumes meaningful stack on these patterns and the matrix runner does not measure stack growth, so an offline reproducer cannot reliably distinguish vulnerable from patched behavior. |
| libc6 | CVE-2010-4051 | not_reproducible_offline | Adjacent bounded repetitions in regcomp bypassed RE_DUP_MAX handling and crashed the compiler | On noble glibc the patched arithmetic accepts the historical bypass patterns without distinguishing diagnostic; constructing a deterministic offline reproducer that fails on the patched build collapses into the broader regex-DoS class already covered by CVE-2009-5155. |
| libc6 | CVE-2010-3856 | not_reproducible_offline | LD_AUDIT was insufficiently restricted, allowing privilege escalation via unsafe DSOs in trusted directories | Same secure-exec/setuid constraint as CVE-2010-3847; the validator does not run privileged binaries and the loader's audit-DSO policy gate is unreachable in unprivileged mode. |
| libc6 | CVE-2010-3847 | not_reproducible_offline | $ORIGIN expansion in LD_AUDIT loaded attacker-controlled DSOs from arbitrary directories during privileged loads | The vulnerable path runs only during the dynamic linker's secure-execution mode; reproducing it requires a setuid binary plus LD_AUDIT manipulation, neither of which the unprivileged validator container can exercise. |
| libc6 | CVE-2010-0296 | not_reproducible_offline | addmntent / mntent encoding path mishandled newline-bearing mountpoint names | Bug class is privileged /etc/mtab corruption from an attacker-controlled mount string; demonstrating the discriminating signal requires writing through a privileged mtab path which the unprivileged validator container does not expose. |
| libc6 | CVE-2010-0015 | not_reproducible_offline | nss_nis getpwnam merged passwd.adjunct.byname data into passwd results, leaking encrypted NIS passwords | Reproducer requires a configured NIS server publishing both the passwd and passwd.adjunct.byname maps plus an attacker-controlled lookup; the validator does not run an NIS infrastructure and cannot drive the nss_nis backend offline. |
| libcurl | CVE-2003-1605 | not_reproducible_offline | libcurl advisory: Proxy Authentication Header Information Leakage (severity High) | Vulnerable behavior surfaces only when an authenticated request is rerouted, redirected, or proxy-tunneled across origins; the validator does not host the multi-origin HTTP/proxy infrastructure required to leak credentials in a discriminating way. |
| libcurl | CVE-2007-3564 | not_reproducible_offline | libcurl advisory: GnuTLS insufficient cert verification (severity Low) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2009-0037 | not_reproducible_offline | libcurl advisory: Arbitrary File Access (severity Medium) | Discriminating signal is the bytes curl emits over the wire after URL canonicalization; the offline validator cannot capture the on-wire request line, only the curl exit status, so most canonicalization regressions cannot distinguish vulnerable from patched output. |
| libcurl | CVE-2009-2417 | not_reproducible_offline | libcurl advisory: embedded zero in cert name (severity High) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2010-0734 | not_reproducible_offline | libcurl advisory: data callback excessive length (severity High) | Bug surfaces as wall-clock or memory degradation under crafted server response patterns or large-input streaming; the matrix runner does not host the adversarial server and uses a coarse exit-status-only signal that cannot discriminate the bounded resource regression. |
| libcurl | CVE-2011-2192 | not_reproducible_offline | libcurl advisory: inappropriate GSSAPI delegation (severity Medium) | Bug requires GSSAPI / Kerberos credential delegation against a configured KDC plus a remote service; the validator does not run a KDC and cannot exercise the delegation policy branch. |
| libcurl | CVE-2011-3389 | not_reproducible_offline | libcurl advisory: SSL CBC IV vulnerability (severity High) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2012-0036 | not_reproducible_offline | libcurl advisory: URL sanitization vulnerability (severity High) | Discriminating signal is the bytes curl emits over the wire after URL canonicalization; the offline validator cannot capture the on-wire request line, only the curl exit status, so most canonicalization regressions cannot distinguish vulnerable from patched output. |
| libcurl | CVE-2013-1944 | not_reproducible_offline | libcurl advisory: cookie domain tailmatch (severity High) | Discriminating signal is cookie / HSTS state propagation across origins, which requires running a multi-origin HTTP server pair; the matrix runner does not stand up the origin set needed to drive the formerly-vulnerable cookie scope. |
| libcurl | CVE-2013-4545 | not_reproducible_offline | libcurl advisory: cert name check ignore OpenSSL (severity Medium) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2013-6422 | not_reproducible_offline | libcurl advisory: cert name check ignore with GnuTLS (severity Medium) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2014-0015 | not_reproducible_offline | libcurl advisory: reuse of wrong HTTP NTLM connection (severity Medium) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2014-0138 | not_reproducible_offline | libcurl advisory: wrong reuse of connections (severity Medium) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2014-0139 | not_reproducible_offline | libcurl advisory: IP address wildcard certificate validation (severity Medium) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2014-1263 | not_reproducible_offline | libcurl advisory: not verifying certs for TLS to IP address / Secure Transport (severity Medium) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2014-2522 | not_reproducible_offline | libcurl advisory: not verifying certs for TLS to IP address / Schannel (severity Medium) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2014-3613 | not_reproducible_offline | libcurl advisory: cookie leak with IP address as domain (severity Medium) | Discriminating signal is cookie / HSTS state propagation across origins, which requires running a multi-origin HTTP server pair; the matrix runner does not stand up the origin set needed to drive the formerly-vulnerable cookie scope. |
| libcurl | CVE-2014-3620 | not_reproducible_offline | libcurl advisory: cookie leak for TLDs (severity High) | Discriminating signal is cookie / HSTS state propagation across origins, which requires running a multi-origin HTTP server pair; the matrix runner does not stand up the origin set needed to drive the formerly-vulnerable cookie scope. |
| libcurl | CVE-2014-8151 | not_reproducible_offline | libcurl advisory: Secure Transport certificate check bypass (severity Medium) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2015-3143 | not_reproducible_offline | libcurl advisory: Reusing authenticated connection when unauthenticated (severity Medium) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2015-3148 | not_reproducible_offline | libcurl advisory: Negotiate not treated as connection-oriented (severity Medium) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2015-3153 | not_reproducible_offline | libcurl advisory: sensitive HTTP server headers also sent to proxies (severity High) | Vulnerable behavior surfaces only when an authenticated request is rerouted, redirected, or proxy-tunneled across origins; the validator does not host the multi-origin HTTP/proxy infrastructure required to leak credentials in a discriminating way. |
| libcurl | CVE-2015-3236 | not_reproducible_offline | libcurl advisory: lingering HTTP credentials in connection reuse (severity High) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2016-0755 | not_reproducible_offline | libcurl advisory: NTLM credentials not-checked for proxy connection reuse (severity Medium) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2016-3739 | not_reproducible_offline | libcurl advisory: TLS certificate check bypass with mbedTLS/PolarSSL (severity High) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2016-4802 | not_reproducible_offline | libcurl advisory: Windows DLL hijacking (severity High) | Bug surfaces only inside platform-specific dynamic-loading paths (Windows DLL search order, OpenSSL engine loading); noble's amd64 Linux container does not exercise the affected platform branch. |
| libcurl | CVE-2016-5419 | not_reproducible_offline | libcurl advisory: TLS session resumption client cert bypass (severity High) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2016-5420 | not_reproducible_offline | libcurl advisory: Reusing connections with wrong client cert (severity Medium) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2016-7141 | not_reproducible_offline | libcurl advisory: Incorrect reuse of client certificates (severity High) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2016-8615 | not_reproducible_offline | libcurl advisory: cookie injection for other servers (severity High) | Discriminating signal is cookie / HSTS state propagation across origins, which requires running a multi-origin HTTP server pair; the matrix runner does not stand up the origin set needed to drive the formerly-vulnerable cookie scope. |
| libcurl | CVE-2016-8616 | not_reproducible_offline | libcurl advisory: case insensitive password comparison (severity Medium) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2016-8624 | not_reproducible_offline | libcurl advisory: invalid URL parsing with '#' (severity Medium) | Discriminating signal is the bytes curl emits over the wire after URL canonicalization; the offline validator cannot capture the on-wire request line, only the curl exit status, so most canonicalization regressions cannot distinguish vulnerable from patched output. |
| libcurl | CVE-2016-8625 | not_reproducible_offline | libcurl advisory: IDNA 2003 makes curl use wrong host (severity High) | Discriminating signal is the bytes curl emits over the wire after URL canonicalization; the offline validator cannot capture the on-wire request line, only the curl exit status, so most canonicalization regressions cannot distinguish vulnerable from patched output. |
| libcurl | CVE-2016-9594 | not_reproducible_offline | libcurl advisory: uninitialized random (severity High) | Discriminating signal is statistical bias in nonce / random output across many invocations, which the matrix runner does not capture; per-call functional output is identical between branches. |
| libcurl | CVE-2016-9952 | not_reproducible_offline | libcurl advisory: Win CE Schannel cert wildcard matches too much (severity Medium) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2017-2629 | not_reproducible_offline | libcurl advisory: SSL_VERIFYSTATUS ignored (severity Medium) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2017-7468 | not_reproducible_offline | libcurl advisory: TLS session resumption client cert bypass (again) (severity High) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2018-1000007 | not_reproducible_offline | libcurl advisory: HTTP authentication leak in redirects (severity Low) | Vulnerable behavior surfaces only when an authenticated request is rerouted, redirected, or proxy-tunneled across origins; the validator does not host the multi-origin HTTP/proxy infrastructure required to leak credentials in a discriminating way. |
| libcurl | CVE-2019-5443 | not_reproducible_offline | libcurl advisory: Windows OpenSSL engine code injection (severity High) | Bug surfaces only inside platform-specific dynamic-loading paths (Windows DLL search order, OpenSSL engine loading); noble's amd64 Linux container does not exercise the affected platform branch. |
| libcurl | CVE-2020-8169 | not_reproducible_offline | libcurl advisory: Partial password leak over DNS on HTTP redirect (severity Medium) | Vulnerable behavior surfaces only when an authenticated request is rerouted, redirected, or proxy-tunneled across origins; the validator does not host the multi-origin HTTP/proxy infrastructure required to leak credentials in a discriminating way. |
| libcurl | CVE-2020-8284 | not_reproducible_offline | libcurl advisory: trusting FTP PASV responses (severity Low) | Discriminating signal is the bytes curl emits over the wire after URL canonicalization; the offline validator cannot capture the on-wire request line, only the curl exit status, so most canonicalization regressions cannot distinguish vulnerable from patched output. |
| libcurl | CVE-2020-8285 | not_reproducible_offline | libcurl advisory: FTP wildcard stack overflow (severity Medium) | Bug surfaces as wall-clock or memory degradation under crafted server response patterns or large-input streaming; the matrix runner does not host the adversarial server and uses a coarse exit-status-only signal that cannot discriminate the bounded resource regression. |
| libcurl | CVE-2020-8286 | not_reproducible_offline | libcurl advisory: Inferior OCSP verification (severity Medium) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2021-22876 | not_reproducible_offline | libcurl advisory: Automatic referer leaks credentials (severity Low) | Vulnerable behavior surfaces only when an authenticated request is rerouted, redirected, or proxy-tunneled across origins; the validator does not host the multi-origin HTTP/proxy infrastructure required to leak credentials in a discriminating way. |
| libcurl | CVE-2021-22890 | not_reproducible_offline | libcurl advisory: TLS 1.3 session ticket proxy host mix-up (severity Low) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2021-22897 | not_reproducible_offline | libcurl advisory: Schannel cipher selection surprise (severity Low) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2021-22924 | not_reproducible_offline | libcurl advisory: Bad connection reuse due to flawed path name checks (severity Medium) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2021-22926 | not_reproducible_offline | libcurl advisory: CURLOPT_SSLCERT mix-up with Secure Transport (severity Medium) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2021-22946 | not_reproducible_offline | libcurl advisory: Protocol downgrade required TLS bypassed (severity Medium) | Discriminating signal is the bytes curl emits over the wire after URL canonicalization; the offline validator cannot capture the on-wire request line, only the curl exit status, so most canonicalization regressions cannot distinguish vulnerable from patched output. |
| libcurl | CVE-2021-22947 | not_reproducible_offline | libcurl advisory: STARTTLS protocol injection via MITM (severity Medium) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2022-22576 | not_reproducible_offline | libcurl advisory: OAUTH2 bearer bypass in connection reuse (severity Medium) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2022-27774 | not_reproducible_offline | libcurl advisory: Credential leak on redirect (severity Medium) | Vulnerable behavior surfaces only when an authenticated request is rerouted, redirected, or proxy-tunneled across origins; the validator does not host the multi-origin HTTP/proxy infrastructure required to leak credentials in a discriminating way. |
| libcurl | CVE-2022-27775 | not_reproducible_offline | libcurl advisory: Bad local IPv6 connection reuse (severity Low) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2022-27776 | not_reproducible_offline | libcurl advisory: Auth/cookie leak on redirect (severity Low) | Vulnerable behavior surfaces only when an authenticated request is rerouted, redirected, or proxy-tunneled across origins; the validator does not host the multi-origin HTTP/proxy infrastructure required to leak credentials in a discriminating way. |
| libcurl | CVE-2022-27779 | not_reproducible_offline | libcurl advisory: cookie for trailing dot TLD (severity Medium) | Discriminating signal is cookie / HSTS state propagation across origins, which requires running a multi-origin HTTP server pair; the matrix runner does not stand up the origin set needed to drive the formerly-vulnerable cookie scope. |
| libcurl | CVE-2022-27780 | not_reproducible_offline | libcurl advisory: percent-encoded path separator in URL host (severity Medium) | Discriminating signal is the bytes curl emits over the wire after URL canonicalization; the offline validator cannot capture the on-wire request line, only the curl exit status, so most canonicalization regressions cannot distinguish vulnerable from patched output. |
| libcurl | CVE-2022-27781 | not_reproducible_offline | libcurl advisory: CERTINFO never-ending busy-loop (severity Low) | Bug surfaces as wall-clock or memory degradation under crafted server response patterns or large-input streaming; the matrix runner does not host the adversarial server and uses a coarse exit-status-only signal that cannot discriminate the bounded resource regression. |
| libcurl | CVE-2022-27782 | not_reproducible_offline | libcurl advisory: TLS and SSH connection too eager reuse (severity Medium) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2022-30115 | not_reproducible_offline | libcurl advisory: HSTS bypass via trailing dot (severity Medium) | Discriminating signal is cookie / HSTS state propagation across origins, which requires running a multi-origin HTTP server pair; the matrix runner does not stand up the origin set needed to drive the formerly-vulnerable cookie scope. |
| libcurl | CVE-2022-32205 | not_reproducible_offline | libcurl advisory: Set-Cookie denial of service (severity Low) | Discriminating signal is cookie / HSTS state propagation across origins, which requires running a multi-origin HTTP server pair; the matrix runner does not stand up the origin set needed to drive the formerly-vulnerable cookie scope. |
| libcurl | CVE-2022-32206 | not_reproducible_offline | libcurl advisory: HTTP compression denial of service (severity Medium) | Bug surfaces as wall-clock or memory degradation under crafted server response patterns or large-input streaming; the matrix runner does not host the adversarial server and uses a coarse exit-status-only signal that cannot discriminate the bounded resource regression. |
| libcurl | CVE-2022-32207 | not_reproducible_offline | libcurl advisory: Non-preserved file permissions (severity Medium) | Bug requires GSSAPI / Kerberos credential delegation against a configured KDC plus a remote service; the validator does not run a KDC and cannot exercise the delegation policy branch. |
| libcurl | CVE-2022-32208 | not_reproducible_offline | libcurl advisory: FTP-KRB bad message verification (severity Low) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2022-32221 | not_reproducible_offline | libcurl advisory: POST following PUT confusion (severity Medium) | Bug requires an HTTP server observing the method/state confusion across a sequence of POST/PUT requests; the validator does not host a configurable HTTP server in scope for libcurl regression cases. |
| libcurl | CVE-2022-35252 | not_reproducible_offline | libcurl advisory: control code in cookie denial of service (severity Low) | Discriminating signal is cookie / HSTS state propagation across origins, which requires running a multi-origin HTTP server pair; the matrix runner does not stand up the origin set needed to drive the formerly-vulnerable cookie scope. |
| libcurl | CVE-2022-42916 | not_reproducible_offline | libcurl advisory: HSTS bypass via IDN (severity Medium) | Discriminating signal is cookie / HSTS state propagation across origins, which requires running a multi-origin HTTP server pair; the matrix runner does not stand up the origin set needed to drive the formerly-vulnerable cookie scope. |
| libcurl | CVE-2022-43551 | not_reproducible_offline | libcurl advisory: Another HSTS bypass via IDN (severity Medium) | Discriminating signal is cookie / HSTS state propagation across origins, which requires running a multi-origin HTTP server pair; the matrix runner does not stand up the origin set needed to drive the formerly-vulnerable cookie scope. |
| libcurl | CVE-2023-23914 | not_reproducible_offline | libcurl advisory: HSTS ignored on multiple requests (severity Low) | Discriminating signal is cookie / HSTS state propagation across origins, which requires running a multi-origin HTTP server pair; the matrix runner does not stand up the origin set needed to drive the formerly-vulnerable cookie scope. |
| libcurl | CVE-2023-23915 | not_reproducible_offline | libcurl advisory: HSTS amnesia with --parallel (severity Low) | Discriminating signal is cookie / HSTS state propagation across origins, which requires running a multi-origin HTTP server pair; the matrix runner does not stand up the origin set needed to drive the formerly-vulnerable cookie scope. |
| libcurl | CVE-2023-23916 | not_reproducible_offline | libcurl advisory: HTTP multi-header compression denial of service (severity Medium) | Bug surfaces as wall-clock or memory degradation under crafted server response patterns or large-input streaming; the matrix runner does not host the adversarial server and uses a coarse exit-status-only signal that cannot discriminate the bounded resource regression. |
| libcurl | CVE-2023-27534 | not_reproducible_offline | libcurl advisory: SFTP path ~ resolving discrepancy (severity Low) | Discriminating signal is the bytes curl emits over the wire after URL canonicalization; the offline validator cannot capture the on-wire request line, only the curl exit status, so most canonicalization regressions cannot distinguish vulnerable from patched output. |
| libcurl | CVE-2023-27535 | not_reproducible_offline | libcurl advisory: FTP too eager connection reuse (severity Medium) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2023-27536 | not_reproducible_offline | libcurl advisory: GSS delegation too eager connection reuse (severity Low) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2023-27538 | not_reproducible_offline | libcurl advisory: SSH connection too eager reuse still (severity Low) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2023-28320 | not_reproducible_offline | libcurl advisory: siglongjmp race condition (severity Low) | Bug surfaces inside multi-handle / multi-thread connection reuse races whose discriminating signal is timing-dependent ownership state across handles; the matrix runner invokes single-shot curl CLI commands and cannot orchestrate the cross-handle race window. |
| libcurl | CVE-2023-28321 | not_reproducible_offline | libcurl advisory: IDN wildcard match (severity Low) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2023-28322 | not_reproducible_offline | libcurl advisory: more POST-after-PUT confusion (severity Low) | Bug requires an HTTP server observing the method/state confusion across a sequence of POST/PUT requests; the validator does not host a configurable HTTP server in scope for libcurl regression cases. |
| libcurl | CVE-2023-38039 | not_reproducible_offline | libcurl advisory: HTTP headers eat all memory (severity Medium) | Bug surfaces as wall-clock or memory degradation under crafted server response patterns or large-input streaming; the matrix runner does not host the adversarial server and uses a coarse exit-status-only signal that cannot discriminate the bounded resource regression. |
| libcurl | CVE-2023-38546 | not_reproducible_offline | libcurl advisory: cookie injection with none file (severity Low) | Discriminating signal is cookie / HSTS state propagation across origins, which requires running a multi-origin HTTP server pair; the matrix runner does not stand up the origin set needed to drive the formerly-vulnerable cookie scope. |
| libcurl | CVE-2023-46218 | not_reproducible_offline | libcurl advisory: cookie mixed case PSL bypass (severity Medium) | Discriminating signal is cookie / HSTS state propagation across origins, which requires running a multi-origin HTTP server pair; the matrix runner does not stand up the origin set needed to drive the formerly-vulnerable cookie scope. |
| libcurl | CVE-2023-46219 | not_reproducible_offline | libcurl advisory: HSTS long filename clears contents (severity Low) | Discriminating signal is cookie / HSTS state propagation across origins, which requires running a multi-origin HTTP server pair; the matrix runner does not stand up the origin set needed to drive the formerly-vulnerable cookie scope. |
| libcurl | CVE-2024-0853 | not_reproducible_offline | libcurl advisory: OCSP verification bypass with TLS session reuse (severity Low) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2024-11053 | not_reproducible_offline | libcurl advisory: netrc and redirect credential leak (severity Low) | Vulnerable behavior surfaces only when an authenticated request is rerouted, redirected, or proxy-tunneled across origins; the validator does not host the multi-origin HTTP/proxy infrastructure required to leak credentials in a discriminating way. |
| libcurl | CVE-2024-2379 | not_reproducible_offline | libcurl advisory: QUIC certificate check bypass with wolfSSL (severity Low) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2024-2398 | not_reproducible_offline | libcurl advisory: HTTP/2 push headers memory-leak (severity Medium) | Bug surfaces as wall-clock or memory degradation under crafted server response patterns or large-input streaming; the matrix runner does not host the adversarial server and uses a coarse exit-status-only signal that cannot discriminate the bounded resource regression. |
| libcurl | CVE-2024-2466 | not_reproducible_offline | libcurl advisory: TLS certificate check bypass with mbedTLS (severity Medium) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2024-8096 | not_reproducible_offline | libcurl advisory: OCSP stapling bypass with GnuTLS (severity Medium) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2024-9681 | not_reproducible_offline | libcurl advisory: HSTS subdomain overwrites parent cache entry (severity Low) | Discriminating signal is cookie / HSTS state propagation across origins, which requires running a multi-origin HTTP server pair; the matrix runner does not stand up the origin set needed to drive the formerly-vulnerable cookie scope. |
| libcurl | CVE-2025-0167 | not_reproducible_offline | libcurl advisory: netrc and default credential leak (severity Low) | Vulnerable behavior surfaces only when an authenticated request is rerouted, redirected, or proxy-tunneled across origins; the validator does not host the multi-origin HTTP/proxy infrastructure required to leak credentials in a discriminating way. |
| libcurl | CVE-2025-0665 | not_reproducible_offline | libcurl advisory: eventfd double close (severity Low) | Bug surfaces inside multi-handle / multi-thread connection reuse races whose discriminating signal is timing-dependent ownership state across handles; the matrix runner invokes single-shot curl CLI commands and cannot orchestrate the cross-handle race window. |
| libcurl | CVE-2025-10148 | not_reproducible_offline | libcurl advisory: predictable WebSocket mask (severity Low) | Discriminating signal is statistical bias in nonce / random output across many invocations, which the matrix runner does not capture; per-call functional output is identical between branches. |
| libcurl | CVE-2025-10966 | not_reproducible_offline | libcurl advisory: missing SFTP host verification with wolfSSH (severity Low) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2025-13034 | not_reproducible_offline | libcurl advisory: No QUIC certificate pinning with GnuTLS (severity Medium) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2025-14017 | not_reproducible_offline | libcurl advisory: broken TLS options for threaded LDAPS (severity Medium) | Bug surfaces inside multi-handle / multi-thread connection reuse races whose discriminating signal is timing-dependent ownership state across handles; the matrix runner invokes single-shot curl CLI commands and cannot orchestrate the cross-handle race window. |
| libcurl | CVE-2025-14524 | not_reproducible_offline | libcurl advisory: bearer token leak on cross-protocol redirect (severity Low) | Vulnerable behavior surfaces only when an authenticated request is rerouted, redirected, or proxy-tunneled across origins; the validator does not host the multi-origin HTTP/proxy infrastructure required to leak credentials in a discriminating way. |
| libcurl | CVE-2025-14819 | not_reproducible_offline | libcurl advisory: OpenSSL partial chain store policy bypass (severity Low) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2025-15079 | not_reproducible_offline | libcurl advisory: libssh global known_hosts override (severity Low) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2025-15224 | not_reproducible_offline | libcurl advisory: libssh key passphrase bypass without agent set (severity Low) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2025-4947 | not_reproducible_offline | libcurl advisory: QUIC certificate check skip with wolfSSL (severity Medium) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2025-5025 | not_reproducible_offline | libcurl advisory: No QUIC certificate pinning with wolfSSL (severity Medium) | Vulnerable behavior is observable only against a TLS endpoint exercising the certificate / transport state machine; the offline validator cannot stand up the per-CVE TLS configuration plus the second-tier MITM/peer needed to discriminate patched from unpatched verification. |
| libcurl | CVE-2025-5399 | not_reproducible_offline | libcurl advisory: WebSocket endless loop (severity Low) | Bug surfaces as wall-clock or memory degradation under crafted server response patterns or large-input streaming; the matrix runner does not host the adversarial server and uses a coarse exit-status-only signal that cannot discriminate the bounded resource regression. |
| libcurl | CVE-2026-1965 | not_reproducible_offline | libcurl advisory: bad reuse of HTTP Negotiate connection (severity Medium) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libcurl | CVE-2026-3783 | not_reproducible_offline | libcurl advisory: token leak with redirect and netrc (severity Medium) | Vulnerable behavior surfaces only when an authenticated request is rerouted, redirected, or proxy-tunneled across origins; the validator does not host the multi-origin HTTP/proxy infrastructure required to leak credentials in a discriminating way. |
| libcurl | CVE-2026-3784 | not_reproducible_offline | libcurl advisory: wrong proxy connection reuse with credentials (severity Low) | Bug requires two HTTP/proxy connections in the same handle pool with divergent credentials; reproducing the cross-identity reuse requires multiple authenticated origin servers, which the offline validator does not host. |
| libgcrypt | CVE-2013-4242 | not_reproducible_offline | Private RSA keys recoverable through a Flush+Reload L3 cache side-channel during decryption | Discriminating signal is wall-clock variance and shared-cache observation across two cooperating processes; the matrix runner records a single exit status and cannot capture cache-line timing residues that distinguish constant-time from leaky operations. |
| libgcrypt | CVE-2014-3591 | not_reproducible_offline | ElGamal decryption lacked ciphertext blinding, enabling key extraction via electromagnetic side-channels | Discriminating signal is electromagnetic emission profile during decryption; the validator harness has no physical-layer or sub-microsecond timing instrumentation and cannot distinguish the blinded versus unblinded computation in software. |
| libgcrypt | CVE-2014-5270 | not_reproducible_offline | ElGamal ciphertext normalization weaknesses eased physical key-extraction attacks | Same EM/timing side-channel constraint as CVE-2014-3591; the offline matrix runner cannot observe the physical-layer signal that distinguishes patched normalization from the original code path. |
| libgcrypt | CVE-2015-0837 | not_reproducible_offline | Modular-exponentiation precomputed-table access leaked secret bits via timing | Discriminating signal is per-iteration timing variance across exponent bits; the matrix runner records only end-to-end exit status and cannot capture sub-microsecond timing residues offline. |
| libgcrypt | CVE-2015-7511 | not_reproducible_offline | Improper elliptic-point curve multiplication during ECDH decryption eased EM key extraction | Same EM side-channel constraint as the other curve-multiplication CVEs; functional output is identical between vulnerable and patched builds, so no offline input distinguishes them. |
| libgcrypt | CVE-2016-6313 | not_reproducible_offline | RNG mixing design let attackers derive 160 bits of future state from prior output | Reproducing the prediction requires capturing a long stream of CSPRNG output from the same process and applying the upstream prediction algorithm; the matrix runner does not capture or analyze RNG output streams across invocations. |
| libgcrypt | CVE-2017-0379 | not_reproducible_offline | Curve25519 code did not properly account for side-channel attacks, allowing secret-key recovery | Same timing/cache side-channel constraint as the other curve-side-channel CVEs; functional outputs do not differ between vulnerable and patched code paths. |
| libgcrypt | CVE-2017-7526 | not_reproducible_offline | Cache side-channel against the left-to-right sliding-window RSA implementation could recover the private key | Discriminating signal is shared-cache observation by a co-resident attacker; the validator has no second-process side-channel observer in scope and the functional RSA output is unchanged by the patch. |
| libgcrypt | CVE-2017-9526 | not_reproducible_offline | EdDSA session-key leakage during signing made the long-term key recoverable | Reproducer requires extracting a session key through fault injection or timing observation; the validator harness cannot inject faults and the functional signature output is identical between branches. |
| libgcrypt | CVE-2018-0495 | not_reproducible_offline | ECDSA signing was vulnerable to a cache side-channel mitigated by blinding | Same cache side-channel constraint; the patched and unpatched ECDSA implementations produce indistinguishable signatures on the same input, so no offline output check discriminates them. |
| libgcrypt | CVE-2018-6829 | not_reproducible_offline | Direct ElGamal encryption lacked semantic security under ciphertext-only attack | Distinguishing the patched from unpatched primitive requires statistical analysis across many ciphertexts under chosen plaintext; the matrix runner runs a single deterministic invocation and cannot drive the chosen-ciphertext oracle needed to discriminate. |
| libgcrypt | CVE-2019-12904 | not_reproducible_offline | Portable AES implementation was reported vulnerable to Flush+Reload on platforms without assembly acceleration | Same cache side-channel constraint; the validator runs on amd64 with assembly-accelerated AES so the affected portable path is not exercised, and the functional ciphertext is identical regardless. |
| libgcrypt | CVE-2019-13627 | not_reproducible_offline | Minerva attack exploited timing leakage related to scalar bit length during ECDSA | Discriminating signal is sub-microsecond timing variance across signing operations; the matrix runner has no high-resolution timing instrumentation and the signatures themselves are functionally indistinguishable. |
| libgcrypt | CVE-2021-33560 | not_reproducible_offline | ElGamal encryption lacked exponent blinding and used an inappropriate window size, enabling side-channel attacks | Same exponent-blinding side-channel constraint; functional outputs are identical between branches, so no offline reproducer can discriminate the patched blinding from the original code path. |
| libgcrypt | CVE-2021-40528 | not_reproducible_offline | Specific public-key parameter and ephemeral-exponent combinations enabled plaintext recovery via interaction with a vulnerable peer | Reproducer requires a cooperating ElGamal peer driving the chosen-parameter interaction; the validator does not run a counterparty protocol service and cannot drive the multi-message attack offline. |
| libgcrypt | CVE-2024-2236 | not_reproducible_offline | Timing side-channel in RSA enabled a Bleichenbacher-style ciphertext-decryption attack | Distinguishing the patched from unpatched RSA padding-oracle behavior requires high-precision timing measurement of many decryption operations; the matrix runner does not instrument per-call timing and the functional return values are identical. |
| libsdl | CVE-2022-4743 | not_reproducible_offline | GLES_CreateTexture leaked GPU resources on early-return paths in SDL_render_gles.c | The discriminating signal is GPU resource accounting after a partial failure inside GLES texture creation; the validator runs without OpenGL ES drivers in headless containers, so the renderer-gles backend is not exercised and the formerly-vulnerable code path is unreachable. |
| libsdl | CVE-2020-14409 | not_reproducible_offline | SDL_BlitCopy integer overflow on crafted BMP input could corrupt the heap | The patched SDL rejects malformed BMPs at the loader before reaching SDL_BlitCopy; constructing a structurally valid BMP that survives the loader and exercises the formerly-vulnerable copy arithmetic offline requires fixture generation beyond what hand-rolled BMPs can deterministically achieve on noble. |
