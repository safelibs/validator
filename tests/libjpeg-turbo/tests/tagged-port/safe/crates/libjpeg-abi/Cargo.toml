[package]
name = "libjpeg-abi"
version.workspace = true
edition.workspace = true
publish.workspace = true
build = "build.rs"

[lib]
path = "src/lib.rs"
crate-type = ["rlib", "staticlib"]

[dependencies]
ffi-types = { path = "../ffi-types" }
jpeg-core = { path = "../jpeg-core" }
