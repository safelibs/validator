# Keep the staged runtime override pinned to the renamed libturbojpeg package
# in both the staged and Debian-package flows.
# upstream (DRC <dcommander@users.sourceforge.net>) states that the below issue
# can be ignored, see:
# http://bugs.debian.org/cgi-bin/bugreport.cgi?bug=612341#255
libturbojpeg: exit-in-shared-library [usr/lib/*/libturbojpeg.so.0.2.0]
