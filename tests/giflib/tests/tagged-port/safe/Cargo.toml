[package]
name = "giflib-safe"
version = "0.1.0"
edition = "2021"
publish = false
build = "build.rs"

[lib]
name = "gif"
crate-type = ["cdylib", "staticlib"]

[profile.release]
codegen-units = 1
lto = "thin"

[dependencies]
libc = "0.2"
